[package]
name = "scene-os-web"
version = "0.1.0"
edition = "2021"
description = "Theorem SceneOS renderer bundle (Lane B): embeds the self-contained canvas renderer and serves a scene-package-v2 as one HTML asset, the SERP injection pattern."
license = "MIT"
rust-version = "1.85"

[dependencies]
serde_json = "1.0"
scene-os-core = { path = "../scene-os-core" }
