/* bindgen entry point: GraphBLAS + LAGraph (stable) + LAGraphX. Stable LAGraph
 * covers BFS, PageRank, betweenness, triangle count, connected components, and
 * SSSP; LAGraphX adds k-truss (LAGraph_KTruss). */
#include <GraphBLAS.h>
#include <LAGraph.h>
#include <LAGraphX.h>
