[package]
name = "rustyred-thg-compat-server"
version = "0.5.0"
edition = "2021"
description = "Standalone HTTP control server for THG-Core."
license = "MIT"
rust-version = "1.85"

[dependencies]
serde_json = "1.0"
rustyred-thg-core = { path = "../rustyred-thg-core", features = ["redis-store"] }
