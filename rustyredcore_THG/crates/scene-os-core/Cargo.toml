[package]
name = "scene-os-core"
version = "0.1.0"
edition = "2021"
description = "Rust SceneOS atom, catalog, selection, and package compiler contracts for Theorem."
license = "MIT"
rust-version = "1.85"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
