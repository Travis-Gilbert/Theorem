[package]
name = "theorem-prompt"
version = "0.1.0"
edition = "2021"
description = "Cache-aware prompt rendering for Theorem head invocations."
license = "MIT"
repository = "https://github.com/Travis-Gilbert/Theorem"
rust-version = "1.85"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
theorem-harness-core = { version = "0.1.0", path = "../theorem-harness-core" }
