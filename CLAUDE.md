# Theorem

Theorem is the **Rust-native substrate spine**: the programmable Rust projection that mirrors the Theseus / Index-API workspace. It carries the RustyRed/THG substrate engine, the PyO3 bridge and parity surfaces that exercise it, the substrate-native browser, the RustyWeb crawler/search engine, and the symbolic-engine plans.

This file is the navigation truth for the repo. Read it first. Order of truth: this map, then the actual code, then the plan docs (plans lag code). If a session changes architecture (new crate, renamed module, removed subsystem, new entrypoint), update this file before ending the session.

## The Mirror Rule (read this before touching anything)

- **Theseus is canonical.** Theseus = the Python + Memgraph + Postgres application/runtime (separate repo: `github.com/Travis-Gilbert/Theseus`, the local `Index-API` checkout). Theorem **mirrors** the Rust projection and its bridge contracts so Rust-side work can move at its own cadence while staying auditable against the source workspace.
- **Do not let Theorem and Theseus diverge silently.** The Python files here (`apps/notebook/`, `apps/orchestrate/`) are mirrors of their Theseus counterparts. Changes that affect the contract (parity receipts, affordance gates, the PyO3 surface) must stay reconciled with Theseus.
- Last sync recorded in `README.md` (2026-06-27): Harness Console gained the CommonPlace code-workspace shell, OpenUI/block-view registry, RustyRed data contract route, and omnibar agent run path; crate publishing gained the layered release workflow; `theorem-grpc` gained recovery-first bind, `/health`, `/ready`, and remote-doctor probes; the adaptive index spine landed in core/server/MCP/GraphQL; binary reconstruction added loader/disasm/lift/reconstruct/harness crates plus `reconstruct_binary`; code intelligence gained engineering compiler, program-analysis, native-loader, and Ghidra-oracle contracts; RedCore document sync/memory-pressure fixes and CommonPlace `content_core` extraction fixes landed. The GraphBLAS sparse-matrix compute core also landed: `crates/rustyred-thg-graphblas` (SuiteSparse:GraphBLAS v9.4.5 + LAGraph v1.2.1 FFI/safe layer, masked `mxv`/`mxm` semiring traversal, the seven LAGraph algorithms) plus `rustyred-thg-core`'s optional `graphblas` feature (typed adjacency over `graph_csr` + hook updates, CFL-reachability points-to, hybrid graph plan nodes). The DATAWAVE-style intake landed in `rustyred-thg-datawave` (`docs/plans/datawave-ingest-edge/`): record -> normalized field-facts + declared entity-edges + a self-describing dictionary + cell visibility/masking + content/fuzzy hashing, the general front door the binary-reconstruction facts compose with, parity-grounded against DATAWAVE's asserted CSV/JSON fixtures @ `integration`; plus the cardinality-tiered global+field index (`tiered.rs`, boolean pushdown), ssdeep-compatible fuzzy hashing (`fuzzyhash`), parity-receipt export into the shared training stream (`training.rs`), and the agent-facing `theorem.ingest.datawave` capability pack in `rustyred-thg-datawave-harness`.
- Prior sync recorded in `README.md` (2026-06-22; added `crates/rustyred-thg-intake`: CommonPlace source intake (`SPEC-COMMONPLACE-SOURCE-INTAKE-AND-ROUTING`, Layer A + the C2 act seam): the `SourceSpoke` ingestion-spoke framework, the `sync_source` scoped/incremental driver, the `RecordTransport`/`FixtureTransport` seam, the `MappedSpoke` universal-contract path, five curated spokes (Gmail/GSuite/Outlook/Notion/Linear), and the `ActSeam` residue->delegation wiring. The same pass extended `crates/commonplace` with Layer B/C: `ItemKind::Task` + task scalars/edges/queries, `Item.source_ref` + idempotent ingest (A3), `IngestPipeline::ingest_batch` (A4) + `ingest_routed`, and `organize.rs` (two-tier `decide` + `RoutingRule`/`route` + soft source prior). 17 commonplace + 20 intake tests green, clippy-clean. Prior 2026-06-20 sync added `apps/harness-console`, the Theorems Harness web console: a greenfield Next.js 16 + React 19 + Tailwind front door over the harness MCP/HTTP backend (one typed `HarnessClient`, mock + live impls), with ten authenticated surfaces + Browser-Use-style onboarding, the 4x4 design-math token file, the DotGrid depth system, the Dynamic Island spine (omnibar + TOC + RustyWeb search + Cmd-K), cosmos.gl Lane A graph, and a real Yjs collaborative agent IDE; `next build` green). Prior 2026-06-19 sync added `crates/commonplace` + `apps/commonplace-api`, `apps/rustyred-embedded`, and `crates/theorem-copresence`.
- Open question still live in `Theseus/Theorem.md`: whether a good tool built on the Theorem (Rust) layer becomes a **promotion candidate** to the canonical Theseus layer. Treat Rust-side tools as candidates, not canon.

## Repository Layout

| Path | What it is |
|------|------------|
| `rustyredcore_THG/` | The RustyRed / THG Rust workspace **and** the PyO3 bridge. A real Cargo workspace (`Cargo.toml [workspace]`). Root lib (`src/`) is the PyO3 module exported to Python as `theseus_native` (maturin). Has its own `Dockerfile` + `railway.toml`. |
| `rustyredcore_THG/crates/` | The graph-engine crates (see table below). |
| `apps/browser/` | `theorem-browser`: the Servo-embedded substrate-native browser. Standalone Cargo crate (NOT in the rustyredcore_THG workspace). |
| `apps/browser-substrate/` | `theorem-browser-substrate`: the Servo-free page->substrate seam. Standalone crate; depends only on `rustyred-web` + `rustyred-thg-core` (no Servo), so it builds + tests in seconds. |
| `apps/rustyred-embedded/` | `rustyred-embedded`: North Star E0 embedded mode (E0.1 linkable core + E0.2 restart rehydration landed). Link the crate and run RustyRed in-process over a local directory, no server: `Engine::open(dir, EmbeddedConfig)` owns a durable `RedCoreGraphStore` behind the in-process `SharedStore` seam (`rustyred-thg-mcp`) and exposes the full typed GraphQL surface as local `query`/`mutate`/`introspect` calls (synchronous, no socket, no async runtime). Restart rehydration rides `RedCoreGraphStore::open`->`recover`, proven through the agent surface. E0.3 adds the single stdio binary (`src/main.rs`: opens an Engine over a data dir, serves MCP JSON-RPC over stdin/stdout, no socket; `Engine::handle(request)` drives the full MCP surface) + `tests/stdio.rs`. No server framework in the tree (no axum/tonic; transitive reqwest/hyper is an HTTP client via rerank/harness-runtime -- feature-gating for a minimal binary is a named follow-up). E0.4 adds the TOML config (`EmbeddedConfig::from_toml_str`/`load_for_dir`; theorem.toml lives in the data dir, configures tenant/durability/graphql_default_surface/code_embedding; data_dir is the binary arg/env). E1 (stream transport) confirmed: stream_publish->stream_read round-trips through `Engine::handle` in-process (local live-tail, no server). E0.5 folder tree: the Engine owns a serde-persisted `DocTree` + `DiskObjectStore`; `fs_write` (content hash + inline/overflow + a durable `file:{path}` graph node), `fs_read` (point lookup), `fs_ls` (prefix scan), plus W6 `fs_unlink`/`fs_rename` (durable `NodeDelete` for old `File` metadata and restart-proven path moves) and explicit directory markers (`fs_mkdir`/`fs_rmdir`/`fs_is_dir`/`list_directories`, durable without zero-byte File nodes) -- the folder tree IS the agent's filesystem, durable across restart. (E0.5 was NOT blocked on B3: DocTree is serde-able + exported; B3's DocTree-as-ColdIndex gap is a separate store concern.) E0.5 fully complete: a written file ALSO gains an embedding (designated File/embedding vector property), so it is vector-searchable; W4.1 routes that through the shared `rustyred-code-embedding` seam, preserving the deterministic hash default while allowing configured HTTP/real encoders with dimension-matched designations, and `Engine::reembed_files(prefix)` explicitly refreshes existing File vectors after an encoder/model/dimension change. W1 adds an opt-in `FileWriteHook` seam that runs after `fs_write`/`fs_write_batch` commits File nodes and persists the DocTree; domain crates can attach post-write maintenance without adding dependencies to embedded core. **Area E COMPLETE: E0.1-E0.5 + E1, 17 lib tests + stdio green, clippy-clean.** Standalone Cargo root (bare `[workspace]`), path-deps into `rustyred-thg-{mcp,core}` plus `rustyred-code-embedding`. Spec: `docs/plans/rustyred-multimodel/E0-embedded-mode.md`; loop status + North Star scorecard + gated remainder: `docs/plans/rustyred-multimodel/LOOP-STATUS.md`. |
| `apps/rustyred-fuse/` | `rustyred-fuse`: standalone W6 filesystem proof for `docs/plans/rustyred-code-workspace/`. The default build is pure Rust and macFUSE-free: it tests the DocTree-to-directory/inode translation over a `FileSource` without linking a platform FUSE library. The optional `mount` feature enables the `fuser` kernel-mount glue for hosts that deliberately install macFUSE/libfuse; the no-macFUSE PR path does not depend on that feature. |
| `apps/rustyred-git/` | `rustyred-git`: W2 git-as-truth layer for `docs/plans/rustyred-code-workspace/`. Standalone Cargo root with `WorkspaceRepo` over the installed git CLI for the full local VCS slice (init, commit-all, branch, checkout, merge conflict surfacing, push to bare, clone) and `GixWorkspaceRepo` as the pure-Rust `gix` backend slice for init/open/head/current-branch/read-at-HEAD/clone plus materialized-tree commit-all through direct gix blob/tree/commit writes, `stage_all_from_worktree`, force branch checkout/rematerialization from the selected committed tree, clean divergent merge commits, unresolved conflict surfacing without advancing HEAD, tree-level branch/worktree diff, `GixWorktreeStatus` for materialized-worktree commit decisions, `GixIndexStatus` for staged/unstaged/untracked porcelain, branch snapshot materialization for isolated parallel-agent work, `GixPreparedPush` push-ready ref update/object closure/packfile generation (validated branch refspec, expected old head, new head, minimal objects to send, parser-verified V2 packfile, decoded receive-pack request body, non-fast-forward rejection), local bare push by consuming that closure plus fast-forward branch ref update, local receive-pack transport, and GitHub-compatible Basic-auth smart-HTTP receive-pack push. Also includes the GitHub PR-open REST seam (`GitHubClient::create_pull_request`) with bearer-token injection for reuse by the existing GitHub App token source; mock REST coverage proves method/path/headers/body/receipt, and the ignored live GitHub push/PR smoke is green against `Travis-Gilbert/Theorem` with draft PR #33. |
| `apps/rustyred-workspace/` | `rustyred-workspace`: W0 code-workspace import seam plus the W3 execution bridge for `docs/plans/rustyred-code-workspace/`. Standalone Cargo root that walks a materialized git checkout with gitignore awareness, filters source vs artifacts/hidden files, and batch-writes source files into `rustyred-embedded` through `Engine::fs_write_batch`; remote URL imports reuse CodeCrawler's credentialed `stage_repo_for_ingest_with_credential` seam. W1 adds `install_code_index_file_write_hook`, an opt-in embedded File-write hook installer that feeds direct source writes into CodeCrawler indexing outside W3 sync-back. W3 adds `materialize_workspace`, `run_tool`, `sync_back_sources`, `sync_back_sources_indexed`, `run_workspace_in_sandbox`, and `run_workspace_in_sandbox_streaming`: project a DocTree subtree into a real OS directory, run a real toolchain with deny-by-default env inheritance and timeout, sync only changed source files back while `target/`/`.git`/hidden/artifact paths stay out of the DocTree, optionally feed changed source bytes into W1 CodeCrawler indexing, drive the same source-rewrite loop through the receiver `SandboxRuntime` contract (`put_files`/`run`/`get_files`) via `LocalProcessSandbox`, and prove streamed stdout can cancel a run while preserving sync-back for source edits made before cancellation. W6 adds `DocTreeMount`, a platform-neutral mount core for read/list/getattr/write/mkdir/unlink/rmdir/rename over the W0 Engine seam with artifact throwaway receipts, synthetic directory prefix rename, persistent empty directory markers, rmdir outcome mapping, and W3 materialization of empty directories, plus an opt-in `fuse-host` feature exposing a `fuser` host adapter over a `DocTreeFuseBackend: Send + Sync` seam and `EngineFuseBackend`, which owns the real embedded `Engine` on a single-threaded actor so FUSE ops and setup-installed File-write hooks run against the real DocTree without making `SharedStore<Rc<RefCell<_>>>` thread-safe; the host also has configurable POSIX attr policy, recursive inode cleanup for forgotten directories, temp-backed throwaway storage for artifact paths, source/artifact rename crossings including whole directory trees that move bytes between throwaway disk and DocTree correctly, and write-buffer cleanup when inodes are forgotten or removed; live macFUSE/fuse3 mounting and mounted execution parity remain follow-ups. No server framework dependency in the embedded crate path; live OpenSandbox smoke and sidecar-backed streaming/cancellation remain W3 follow-ups. |
| `apps/theorem-ios/` | SwiftPM scaffold for the native Theorem iOS client. Holds the SwiftUI shell, Dynamic Island control surface, ScenePackageV2 models, mobile projection switcher, local smoke executable, and the pre-Xcode compile path. Full simulator/archive work still needs Xcode. |
| `apps/theorem-harness-server/` | Standalone Axum JSON/HTTP transport over `theorem-harness-runtime` and `RedCoreGraphStore`. Serves run list/detail plus room status, presence, live intents, actor mentions, and room records for iOS/web harness surfaces. Own Cargo root, not a `rustyredcore_THG` member. |
| `apps/theorem-grpc/` | `theorem-grpc`: Theorem's first gRPC server. Serves `theseus_search.v1.SearchService` in pure Rust over the RustyRed substrate (`rustyred-web` search + `rustyred-thg-core` store/PPR). No PyO3, no Django. Standalone Cargo root (NOT a `rustyredcore_THG` member); own `Dockerfile` + `railway.toml` (its own Railway service). URL-swap target for civic-atlas-server's `SearchServiceClient` via `THEOREM_SEARCH_URL`. Binds `[::]:$PORT` (default `50071` local) and merges gRPC with HTTP `/health` liveness plus `/ready` readiness on the same listener; RedCore code/app store recovery runs in the background, so `/health` stays alive while `/ready` returns recovering/failed until durable stores are usable. Also exposes Theorems Harness remote-doctor probes at `/.well-known/theorems-harness/doctor.json`, `/diagnostics/queue`, `/diagnostics/dependencies`, and `/diagnostics/tenants`. |
| `apps/theorem-gateway/` | `theorem-gateway`: browser-facing GraphQL gateway (Axum + async-graphql + tonic) whose resolvers call `theorem-grpc` (`SearchService` + `CodeCrawlerService`) and the GL-Fusion model endpoint, shaping results for the web. Stores nothing: a front door + translator mirroring `our-civic-atlas-backend`. Generates tonic CLIENT stubs from vendored protos (`theseus_search/v1`, `theorem_code/v1`); path-deps into `rustyredcore_THG` only for the SceneOS add-on (`scene-os-core` compile + `scene-os-web` serve), so the Dockerfile copies the whole crates dir. Standalone Cargo root; own `Dockerfile` + `railway.toml` (its own Railway service, public domain). `POST /graphql` + GraphiQL on `GET /graphql` + `/healthz`; binds `0.0.0.0:$PORT` (default `50080`). Per-IP token-bucket rate limiting (in-memory or Valkey) + a public ingest allowlist guard the browser boundary. Showpiece `askAgent` returns the GL-Fusion answer next to the graph context that fed it. SceneOS add-on: `sceneForInput` compiles a force-graph `ScenePackageV2` (community/centrality/annotation atoms) via `scene-os-core`; `GET /scene/{id}` serves it as one self-contained HTML asset via `scene-os-web` (explosion->settle->annotate). `schema.graphql` committed for frontend codegen. |
| `apps/ios/TheoremKit/` | Swift Package (`TheoremKit`), distinct from `apps/theorem-ios`. Shared kit layer. |
| `apps/commonplace-api/` | `commonplace-api`: the CommonPlace interoperability API seam (consumer-loop plan unit F3). A typed consumer GraphQL profile over the `commonplace` object model (Query: item/items/collection/collections/collectionItems/search; Mutation: ingest/putNote/editItem/createCollection/addToCollection), gated by per-instance API keys (`x-api-key` header -> `ApiKeyToken` request data -> `ApiKeyRegistry`; every resolver authorizes before any store access, invalid key rejected). Axum + async-graphql 7 (axum 0.8, matching async-graphql-axum); `POST /graphql` + GraphiQL on `GET /graphql` + `/healthz`; binds `0.0.0.0:$PORT` (default 50090). Standalone Cargo root (bare `[workspace]`, like `apps/theorem-grpc`); path-deps into `rustyredcore_THG/crates/{commonplace,rustyred-thg-core}` and consumes ONLY commonplace's public API (zero-collision lane). Backing is selectable: in-memory by default (each instance = its own dataset), or durable `RedCoreGraphStore`+`DiskObjectStore` (AofAlways) when `COMMONPLACE_DATA_DIR` is set -- the GraphQL schema + both bins are generic over the store (`Query<S,B>`/`build_schema<S,B>`/`redcore_store(dir)`), and put/edit are durable across a real process restart (`tests/durable_{http,mcp}_acceptance.rs`). KNOWN GAP (RedCore core lane, surfaced to Codex `msg_c090392d1f356c88`): the multi-write `ingest` sequence is not crash-durable across an abrupt restart (single writes + in-process reopen are) -- `#[ignore]` target `ingest_survives_restart_blocked_on_redcore`. 3 acceptance tests (read/write with a key; invalid+missing key rejected; separate instances = separate data) + live HTTP smoke (authed ingest auto-structures over HTTP, unauthed rejected); clippy-clean. **I1 (ask-over-store)** added: `src/retrieve.rs` + an `ask(question, k)` query that runs unified retrieval over the consumer store fusing vector (the engine embedding index), lexical (in-crate idf token-overlap = the FTS arm), and graph (`SIMILAR_TO` propagation) via reciprocal-rank fusion, returning per-item provenance (which arms surfaced each) + an answer from an `AnswerModel` seam (`NoModel` -> honest extractive answer; generative GL-Fusion/RunPod is the follow-up). live HTTP `ask` smoke. **I2 (proactive briefing)**: `src/briefing.rs` + a `briefing` query (recent by `updated_at` / newly-connected by `SIMILAR_TO` degree / open-threads = open-tagged or unfiled). **I4 (discovery)**: `src/discover.rs` + a `discover` query (vector-similar item pairs not yet `SIMILAR_TO`-linked, ranked; excludes connected pairs). **AG3 (MCP access)**: `src/mcp.rs` + a second `commonplace-mcp` stdio binary speaking newline-delimited JSON-RPC 2.0 (`initialize`/`tools/list`/`tools/call`) exposing put_note/ingest/get_item/list_items/search, so any model reads/writes items over MCP. **X2 (import/export)**: `src/portability.rs` + an `export(format: JSON|MARKDOWN)` query (lossless JSON / human-readable markdown) and an `importItems(data)` mutation that recreates items + collections with their original ids so memberships survive a round-trip. **Organize (auto-organize surface)**: `src/organize.rs` + an `organize(needsYouCeiling, timeframe)` query that partitions timeframe-arrived items into `needsYou` (below the confidence ceiling OR time-sensitive) vs `organizedToday` (filed confidently, grouped by collection) with a reconciling `dailyProgress`, reusing the engine's classification (new public `IngestPipeline::classify_item` ranks collections by cosine to their label embeddings). Confidence is measured against ESTABLISHED (>1-member) collections, so a novel item that only seeded its own singleton at ingest (F2 files everything) surfaces for a decision rather than reading as filed. ISO timestamps via an in-crate proleptic-Gregorian helper (no date dep). Powers the `travisgilbert.me` Auto Organize page (the `daily` screen): `views/AutoOrganizeView.tsx` consumes it via `gqlOrganize` through the gateway proxy. 13 api tests (3 F3 + 2 I1 + 1 I2 + 1 I4 + 1 AG3 + 2 X2 + 3 organize) + live HTTP smokes + an MCP-stdio acceptance; clippy-clean. Follow-ups: crash-durable `ingest` (RedCore core gap above), native FTS (core `FullTextIndex`/tantivy) over the lexical arm, personalized-PageRank over the discovery/briefing graph arm, GL-Fusion/RunPod generative answers + real embeddings (the `AnswerModel` + `Embedder` seams are in place), Railway deploy. |
| `apps/commonplace-clipper/` | `commonplace-clipper`: browser extension fork of `obsidianmd/obsidian-clipper` for CommonPlace push capture. Keeps the upstream reader, highlighter, extraction, template, and interpreter stack, and adds a CommonPlace save target beside the Obsidian URI writer. The popup can save to CommonPlace, Obsidian, or both; CommonPlace posts the `CapturedObject` JSON body with `captureMethod: "clipped"`, source URL, rendered markdown body, and template properties to the configured capture endpoint. |
| `apps/desktop/` | `desktop`: Tauri desktop shell. The Rust backend owns the local harness node (`127.0.0.1:17888`), the CommonPlace API loopback server (`127.0.0.1:17890`), keychain, receiver, browser tabs, coordination/job/model commands, and shutdown. SPEC-9 repoints the main window to the external CommonPlace Next.js frontend export while keeping the existing Vite wrapper as a typed command-contract/reference surface. |
| `apps/copresence-editor/` | `copresence-editor`: a standalone Vite + React + TypeScript browser adapter for the `theorem-copresence` substrate. It uses Velt's Yjs-backed collaboration plus Tiptap so humans and a Gemma-backed co-writer can edit one shared document; the Gemma seam is OpenAI-compatible and base-URL-swappable. Plan: `docs/plans/copresence-velt-gemma/PLAN.md`. |
| `apps/harness-console/` | `harness-console`: standalone Next.js 16 / React 19 control surface for Theorems Harness at `harness.theoremsweb.com`. Greenfield app, separate from Context-Theorem-UI, with Agent, Memory, Skills, Rooms, Runs, API Keys, Providers, Usage, Connections/MCP Hub, Settings, collaborative CodeMirror/Yjs editor, cosmos.gl memory graph, Dynamic Island omnibar, tokenized 4px design-math lint, and the CommonPlace code-workspace shell. The CommonPlace surface now carries the OpenUI/block-view registry, code-agent contract + `/api/theorem/agent`, RustyRed data contract + `/api/commonplace/rustyred`, deck.gl geo preview, and an omnibar path that can run a theorem agent. |
| `apps/theorem-harness-node/` | Node.js (NAPI-RS) binding over the `theorem-harness` Rust SDK. Thin native addon generated from the same core SDK so the JavaScript surface stays aligned with the durable RedCore harness. |
| `apps/theorem-harness-swift/` | Swift (UniFFI) binding over the `theorem-harness` Rust SDK. Generates Swift/Python bindings and an iOS-consumable package/xcframework from the same core surface as the Rust and Node SDKs. |
| `apps/web/` | Empty local web staging directory seen by full doc-drift checks. It is not a tracked product surface yet; keep product web work in `apps/harness-console/` or `apps/theorem-gateway/` until this directory has a manifest and code. |
| `apps/obsidian-sync/` | `theorem-harness-sync`: device-side Obsidian community plugin (TypeScript, esbuild). Mirrors the tenant's memory docs into a vault (Phase 1 pull) and writes note edits + `[[wikilinks]]` back into the graph (Phase 2 write-back). Talks to the harness over `requestUrl`: `GET /v1/tenants/:tenant/memory/docs` for read and the `upsert_note` MCP tool over `POST /mcp` for write. Three echo guards (content-hash gate, remote-write suppression, conflict copies). Standalone npm package, not a Cargo member. Plan: `docs/plans/obsidian-sync/`. |
| `apps/jobintel/` | `jobintel`: a standalone job-intelligence + outreach CLI that is a light HTTP *consumer* of a running RustyRed (no path-deps into the substrate; talks only to the public tenant routes). 0.1: ingests HN "Who is Hiring" + Greenhouse/Lever/Ashby ATS into the graph (Company/Role/Skill/Source/Person nodes + edges), designates Role/Profile embeddings as HNSW, ranks roles against a profile by blending vector search + PPR/PageRank + flag boosts, and drafts per-lead context packs (`context/pack`) into `out/queue.md`. 0.2 (`src/outreach/`): an outreach state machine on the Role graph + append-only `OutreachEvent` trail, a Gmail create-draft bridge (writes drafts, never sends; second HTTP seam at `gmail.googleapis.com`, base-URL-swappable for the mock), a day-4/day-9 follow-up cadence with a daily cap, reply detection + `drafted->sent` advancement via `sync`, and a per-template/per-lead-type learning signal (`OutcomeRecord` + `outreach stats`). Standalone Cargo root (bare `[workspace]`), `reqwest::blocking` + clap + chrono + base64. `Embedder` seam has `hash` (offline default), `http` (Theseus SBERT swap), and `bge` (candle bge-small-en-v1.5, feature-gated). Specs: `~/Downloads/jobintel-build-spec.md` (0.1), `~/Downloads/jobintel-0.2-spec.md` (0.2). |
| `apps/notebook/` | **Python** mirror of Theseus's inference layer: `inference_engines/` (symbolic-engine contracts, Python reference engines, native bridge adapters, Gate-0 affordance coverage, tests), `inference_kernel/` (native-vs-Python routing/execution), `benchmarks/` (byte-parity + cost gates), `discovery_runs/` (Rust-theorem callers routing archive/policy-evolution through the native evolution engine). |
| `apps/orchestrate/runtime/` | **Python**: `map_elites_tick.py`, the orchestration tick for native MAP-Elites archive throughput. |
| `Theseus/Theorem.md` | Source framing: Theseus-as-canonical / Theorem-as-Rust-projection, and the promotion-candidate idea. The "why this repo exists" doc. |
| `docs/plans/` | The plan tree (see Status). `rust-theorem-symbolic-engines/`, `rusty-red-web/`, `rustyweb-v1-design/`, `commonplace-substrate-reconciliation/`. |
| `docs/reference/` | Source architecture context, incl. `commonplace-substrate-architecture-part-4-1.md` (symbolic compute offload). |
| `.github/workflows/servo-browser.yml` | CI build for the Servo embedder (heavy; manual trigger). |

### RustyRed/THG crates (`rustyredcore_THG/crates/`)

| Crate | Purpose |
|-------|---------|
| `rustyred-thg-core` | The graph engine core: `GraphStore` trait, the store impls, mutations/transactions, indexes, TTL, AOF durability, symbolic engines (`symbolic.rs`), graph-level mutation hooks (`hooks.rs`: post-commit reactive compute), and the native relational core (`access_method.rs`, `relational.rs`, `planner.rs`, `cold_fragments.rs`, `working_log.rs`). The heart of the substrate. Vector designation changes rebuild the matching vector index when the dimension changes, so W4 re-embed can safely move File/CodeSymbol embeddings between dimensions. RedCore now exposes `delete_node`, a logical durable node removal that journals `RedCoreMutation::NodeDelete` and replays it across AOF/snapshot recovery. Storage spine (cut 6) lives here too: `object_store.rs` (the zstd-compressed disk-backed content-addressed cold object/document store, the cold tail's durable home), `doc_tree.rs` (copy-on-write B-tree cold document namespace with inline leaves and overflow blobs), `cold_index.rs` (the `ColdIndex` catalog trait + in-memory/disk/ordered-native impls: id -> tier/object-hash keyed rehydration), `ordered.rs::EvictionFrontier` (per-scope ordered index over `last_accessed_ms` for O(k log n) coldest-first eviction), and version-neutral `evict_node`/`readmit_node`/`evict_edge`/`readmit_edge` on `GraphStore` (residency changes that never bump `stats().version`, so the PPR cache stays warm). Adaptive-index spine foundations also live here: index manifests, query receipts, advisor proposals, context views, map artifacts, labeled training runs, training exports, vector evals, adaptive decisions, and side-index/document-sync memory-pressure reductions. |
| `rustyred-thg-fuse` | Read-only graph archive filesystem core over `CompiledGraphPack.archive_objects`: exposes `/snapshots/{version}/statements`, `/nodes`, `/edges`, and `/by-subject` paths backed by rkyv zero-copy archive bytes. Default build is pure Rust; optional `mount` feature carries the fuser dependency. |
| `rustyred-web` | RustyWeb. V0 fixture crawler kernel + V2 hardening: URL canonicalization, `a[href]` extraction, BLAKE3 content snapshots, page->graph emission (`build_fixture_crawl_graph` / `build_v2_fixture_crawl`), application into a `GraphStore`, plus the browser-use web primitives (`PageState`, `BrowserAction`, `web_consume_to_graph`) under `browser_engine.rs`. The search path now carries behavior specs and epistemic cascade fixtures for trust-ahead-of-relevance ranking. |
| `rustyred-hipporag` | HippoRAG 2 candidate generation for RustyRed: `Page`/`Phrase`/`Hub` schema, phrase indexing, RAPTOR-style summary hubs via graph hooks, query-specific PPR retrieval, and membrane-compatible `Candidate` output. Reranking/admission stay in `rustyred-rerank` and `rustyred-membrane`. |
| `rustyred-membrane` | Shared context admission and eviction membrane for RustyRed graph-backed context. Used by HippoRAG, RustyWeb, and fractal expansion to admit candidates before they enter the graph-backed context surface. |
| `rustyred-code-embedding` | Shared W4 code embedding seam for file and CodeCrawler write paths: `CodeEmbedder`, selectable `hash` default, feature-gated `http` hosted encoder path with mock coverage, and feature-gated `local` Candle/BGE model loading (`BAAI/bge-small-en-v1.5`, D=384) with a green live ranking smoke. |
| `rustyred-rerank` | Reranker scorer implementations for RustyRed membrane admission, including the lexical cross-encoder seam consumed by search/fractal pipelines. |
| `rustyred-thg-adapters` | Adapters into the core. |
| `rustyred-thg-affordances` | Connector-as-substrate learning registry: MCP connector tools become first-class `Affordance` graph nodes; the substrate learns which affordance to reach for from accumulated invocation outcomes (PPR over `SERVED_TASK`/`PRODUCED_OUTCOME` edges plus fitness), scoped per agent by a `CapabilityScope`. Structural sibling of `rustyred-thg-adapters`; reuses `theorem-harness-core` for the pairformer A/B validation gate. 15 tests green. |
| `rustyred-thg-binformat` | Binary reconstruction loader: parses binary artifacts into observed `BinaryArtifact`, section, symbol, string, relocation, and entrypoint facts and writes them to GraphStore with provenance. |
| `rustyred-thg-code` | Code parsing plugin/runtime for tenant graph stores: parses source into code graph records so CodeCrawler and code-KG surfaces can search, explain, and explore repositories. W1 code-workspace slice adds an opt-in `materialize_symbol_name_index` ingest flag that writes `CodeSymbolName` bucket nodes with deterministic target refs for touched-name lookup, plus `index_source_file_write_in_store` for source-file write indexing, touched outgoing/incoming `CALLS`/`DEPENDS` edge rewrite, and hooked embedding refresh proof without a repo walk; default ingest cost is unchanged until the on-write lane enables it. W4.1 routes `CodeSymbol` embeddings and instant epistemic warm-up through the shared `rustyred-code-embedding` seam, so configured encoder dimensions drive the vector designation instead of a hardcoded symbol dimension. The latest code-intelligence pass adds engineering compiler contracts, program-analysis run graph records, native object-loader evidence, and Ghidra oracle fixtures. |
| `rustyred-thg-connectors` | Live MCP connector transport: the outbound mirror of `rustyred-thg-mcp`. Connects to an external MCP server over stdio, performs the handshake, walks `tools/list`, and registers each tool as a learnable `Affordance` node via `rustyred-thg-affordances::register_connector`. Pure JSON-RPC protocol layer + thin `BufRead`/`Write` stdio transport (tokio-free, in-memory testable) + `connect_and_register` bridge. Slice 2 adds the invoke bridge (`invoke.rs`): `tools/call` -> `record_invocation` gated by `InvokePolicy` (dry-run default, `FireAllowlist` opt-in), with the `ConnectionTarget` persisted on the `Connector` node so a selected tool is reachable again. Closes the affordances plan's "Live MCP tools/list ingestion" seam. 19 tests green. Plan: `docs/plans/mcp-learning-layer/connector-transport-plan.md`. |
| `rustyred-thg-disasm` | Binary reconstruction decoder: uses `iced-x86` for x86-64 executable sections and emits `InstructionFact` nodes with addresses, bytes, flow-control, branch targets, and coarse effects. |
| `rustyred-thg-lift` | Binary reconstruction THIR lifter: lowers instruction facts into `ThirFunction`, basic-block, and SSA-like statement records (`Assign`, `Call`, `Branch`, `Return`, `Raw`) with graph write-through. |
| `rustyred-thg-reconstruct` | Binary reconstruction compiler: derives semantic-role hypotheses, recovers component hypotheses, emits evidence-backed `ReconstructionInstruction` nodes, and records validation receipts. |
| `rustyred-thg-reconstruct-harness` | Harness capability pack `theorem.reconstruct.binary`: exposes reconstruction load/analyze/lift/graph-write/component/plan/instruction/validate/receipt operations without surfacing raw disassembly to agents. |
| `rustyred-thg-datawave` | DATAWAVE-style intake (write-side absorb of `NationalSecurityAgency/datawave`; plan `docs/plans/datawave-ingest-edge/`): turns any source record into typed normalized field-facts plus declared entity-edges in a `GraphStore`, the general front door the binary-reconstruction facts compose with because they share the graph. Sync, trait-generic over `GraphStore` (deps = `rustyred-thg-core` + serde + sha2). All nine DATAWAVE build phases: `RawRecord`/`RecordBody` + a `TypeRegistry`; the keystone `IngestHelper` contract (one record in, normalized field-facts out) over a shared `derive_fields` pipeline (alias -> normalize -> composite -> virtual -> mask) with three data-types: `CsvHelper`, `JsonHelper`, and the config-driven universal `MappedHelper` ("point at a URL/repo/API and ingest by configuration, no bespoke Rust"); a typed normalizer set matched to DATAWAVE's asserted outputs (`FieldType`: LcNoDiacritics text, the `NumericalEncoder` lexicographically-sortable number form `111`->`+cE1.11`, `DateType` `...THH:mm:ss.SSSZ`, zero-padded IP, geo); per-field `IndexPolicy` (indexed/reverse/tokenized/index-only); `CompositeDef` (U+10FFFF separator) + `VirtualDef`/`VirtualTransform` + field aliasing; `materialize_event` writing `IngestEvent`/`FieldFact`/`FieldEntity` nodes where a `vf="field=value"` property rides the existing GraphStore property index as DATAWAVE's value+field global index, with the dedicated `tiered.rs` cardinality-tiered global+field index (value+field->fragment, boolean `intersect`/`union` pushdown) composing over the same facts; `EdgeDef` + `EdgeCondition` boolean gate (present/equals/not-equals/all/any/not) + definition versioning + `derive_edges`; a self-describing data + edge `dictionary`; per-fact `visibility` + `MaskRule` masking; `content_hash` (sha256 dedup, binformat convention) + ssdeep-compatible `fuzzy_hash`/`fuzzy_compare` (the pure-Rust `fuzzyhash` crate) reused for binary similarity; and `training.rs` exporting parity receipts as `DatawaveParityReceipt` nodes + the shared `LabeledTrainingRun`/`TrainingExportRecord` JSONL stream the reconstruction lane feeds. `DatawaveIngest::{ingest_record,ingest_batch,write_dictionary}` is the driver. The Next-Build-Cut parity lane (`tests/parity.rs`) grounds the intake against DATAWAVE's own asserted `my-nci.csv` (CSV field-facts + index policy) and `flattener-test.json` (25-key/29-value NORMAL flatten) fixtures @ `integration`. 33 unit + 2 parity + 1 doc-test green, clippy-clean. The agent surface is the sibling `rustyred-thg-datawave-harness` crate. Remaining optional upgrades (STATUS): regex edge preconditions, BigDecimal-exact long mantissas / geohash forms, full Unicode NFD. |
| `rustyred-thg-datawave-harness` | Harness capability pack `theorem.ingest.datawave`: the agent-facing surface over `rustyred-thg-datawave`, mirroring `rustyred-thg-reconstruct-harness`. A `DatawaveIngestPlugin` (`RustyRedPlugin`) registers five operations: `ingest.describe` (list the pack), `ingest.record` / `ingest.batch` (ingest into field-facts + edges; batch optionally writes the dictionary), and `ingest.lookup` / `ingest.intersect` (read persisted `FieldFact` nodes by value+field through the property index, with AND pushdown). Every operation is data-driven: a serializable `HelperSpec` (csv/json/mapped) configures the data-type in the request, so "point at a source and ingest by configuration" works over the plugin bus with no bespoke Rust. Deps: `rustyred-thg-core` + `rustyred-thg-datawave` + serde; dev-dep tempfile. Proven end to end over a real `RedCoreGraphStore` (describe -> record -> lookup -> intersect, plus the registry dry-run gate); 2 tests green, clippy-clean. |
| `rustyred-thg-compat-server` | Standalone legacy HTTP control server for THG-Core command execution, run lookup, state hash, health, and readiness routes over memory or Redis-backed executors. |
| `rustyred-thg-geotemporal` | Geo + temporal indexing, including the plugin wrapper that registers the planner-facing `time_series` access method for `TimeRange` predicates. |
| `rustyred-thg-fractal` | Native Rust fractal expansion pipeline over RustyRed and RustyWeb. It grows corpus context by crawling/admitting web graph state into a lower-trust quarantined tier rather than doing graph-only retrieval. |
| `rustyred-thg-mcp` | Native Rust MCP server (graph reads/algorithms without a Python process in the loop). It exposes the GraphQL surface, adaptive-index inspection through `rustyred_thg_index_spine`, and binary reconstruction through `reconstruct_binary`. |
| `rustyred-thg-ml` | Shared graph tensor and message-passing primitive crate for THG learned organs: `GraphTensorBatch`, scatter path selection, deterministic fixed-point aggregation, and feature-gated Burn scatter aggregation consumed by adapters. |
| `rustyred-thg-graphblas` | GraphBLAS sparse-matrix compute core (`HANDOFF-GRAPHBLAS-COMPUTE`). A leaf FFI + safe layer over SuiteSparse:GraphBLAS v9.4.5 (Apache-2.0) + LAGraph v1.2.1 (BSD), built compact + JIT via a vendored `graphblas.sh` (`build.rs` uses a prebuilt prefix from `RUSTYRED_GRAPHBLAS_PREFIX` or auto-builds into `~/.cache`; bakes `CMAKE_OSX_SYSROOT` so macOS JIT kernels compile). Owns `Matrix`/`Vector`/`Semiring`/`Monoid`/`BinaryOp`/`Descriptor` handles (built-in vs owned, correct Drop; `unsafe impl Send` for mutex use), masked `mxv`/`mxm` semiring traversal (reachability / min-plus / plus-times), and the seven LAGraph algorithms (BFS, PageRank, betweenness, triangle count, k-truss via LAGraphX, connected components, SSSP). Default dev links libomp dynamically; a static-libomp build path (`graphblas.sh` with `libomp.a`) yields a `libgraphblas.dylib` with no load-time libomp dependency (otool-clean). Deliberately NOT a workspace member (`[workspace] exclude`): membership would force the native build on every `cargo build --workspace`. Pulled in only via `rustyred-thg-core`'s optional `graphblas` feature; tested standalone via `--manifest-path` (15 tests). Build/test need `RUSTYRED_GRAPHBLAS_PREFIX` + `LIBCLANG_PATH`; downstream binaries need `DYLD_FALLBACK_LIBRARY_PATH` (the `cargo:rustc-link-arg` rpath does not propagate across crates). Mirrors FalkorDB's vendoring pattern; copies no GPL/CC-BY-NC/SSPL source. The core-side integration (typed adjacency over `graph_csr` + hook-driven updates, CFL-reachability points-to/taint, hybrid graph plan nodes) lives in `rustyred-thg-core::graphblas_{adjacency,cfl,plan}` behind the `graphblas` feature (7 tests). |
| `rustyred-thg-memory` | Graph-native memory: PPR-seeded `recall`, `consolidate`, `decay`, validity/contradiction, and the project-scope bias. Cut 6 adds the storage-spine eviction tier here: `ColdTier` (bundles a `ColdObjectStore` + `ColdIndex` + `EvictionFrontier`), `evict_decayed` (flag -> commit content object -> record cold index -> `evict_node` -> drop from frontier; reads the coldest tail from the frontier in O(k log n), never the O(n) memory-node scan; version-neutral), `rehydrate` (keyed cold-index lookup -> object -> `readmit_node`, the recall-triggered path), `recall_with_cold_tier` (rehydrate absent seeds + zadd-on-access), and `park_scope`/`unpark_scope` (warm tier: compile a scope to a `CompiledGraphPack`, evict it, rehydrate the whole subgraph via `checkout_graph_version`). Acceptance 1-5 proven in `tests/storage_spine_acceptance.rs`. |
| `rustyred-thg-pg-server` | Postgres wire protocol server for RustyRedCore-THG native views: trust-mode startup/auth, simple query plus Parse/Bind/Describe/Execute/Sync, typed row encoding, executor-backed `nodes` view queries, and SQL-to-`QueryIr` lowering for planner-backed joins. |
| `rustyred-thg-resp-server` | RESP protocol server for ordered-index commands, exposing a Redis-shaped command loop over `OrderedIndexRegistry` for focused protocol compatibility. |
| `rustyred-thg-server` | Product HTTP/gRPC/MCP surface over RustyRedCore-THG: tenant graph routes, query/Cypher APIs, harness coordination, browser actions, fractal expansion, TTL sweeping, adaptive-index spine routes, and memory-pressure diagnostics for RedCore document sync. |
| `rustyred-thg-catalog` | Compatibility sqlx/Postgres catalog for tenants, projects, billing, auth, plus the legacy `cold_index`/`cold_scope` rows. The hot native path now lives in `rustyred-thg-core` via `OrderedColdIndex`, `NativeCatalog`, and the relational planner; keep `PostgresCatalog`/`PostgresColdIndex` for deployments that still need the old external catalog bridge. Runtime sqlx queries (no compile-time `DATABASE_URL`); the crate builds with no live DB, live round-trip is an `#[ignore]` test. |
| `commonplace` | CommonPlace consumer data layer (plan `docs/plans/commonplace-consumer-loop/`). The personal-database object model graph-native over `GraphStore`: `Item` (universal unit; any `ItemKind`, `Residency` field the sync layer reads, inline/blob `ItemBody`), `Collection` (`Collection` node + `IN_COLLECTION` membership edges, enumerable by reverse traversal), `Tag` (`Tag` node keyed by stable slug + `HAS_TAG` edge). The `Commonplace<S: GraphStore, B: BlobStore>` facade maps the model onto graph records; a `File`'s bytes live content-addressed in a `BlobStore` (`InMemoryBlobStore` for tests, the core `DiskObjectStore` for durability; both yield identical `sha256:<hex>` addresses). F1 = 6 tests green (in-memory + RedCore restart rehydration), clippy-clean. F2 = `IngestPipeline` with deterministic local `Embedder` seam: detects note/doc/link/image input, embeds it, classifies/creates auto collections from label embeddings, stores blobs, writes vector-searchable top-level item `embedding`, files folder-path metadata, writes `SIMILAR_TO` edges, and resolves near-duplicate `Entity` nodes. Divergence from the plan's "module over `rustyred-thg-catalog`": graph-native instead, so Items are first-class for F2 classification/I1 retrieve and acceptance stays DB-free; catalog stays the home for tenant/key/billing (F3). **Source-intake-and-routing additions** (`SPEC-COMMONPLACE-SOURCE-INTAKE-AND-ROUTING`): `ItemKind::Task` is now first-class with indexed task scalars (`status`/`priority`/`due_at_ms`) and edges (`SUBTASK_OF`/`DEPENDS_ON`/`ABOUT`/`WORKED_BY`) plus task queries (`open_tasks`/`tasks_due_between`/`subtask_progress`) in `store.rs`; `Item.source_ref` (A3) + the `item_by_source_ref` exact-match lookup make ingest idempotent on `(source, external_id)` (re-ingest reuses the id so outgoing edges upsert, never double); `IngestPipeline::ingest_batch` (A4: one prior-items snapshot + one designation, graph-identical to one-at-a-time) and `ingest_routed` (B1 hard-route); a new `organize.rs` is the two-tier boundary (`decide` -> `AutoFiled`/`FiledForReview`/`NeedsYou`, pure cosine, zero model calls) plus `RoutingRule`/`route` (explicit hard-route) and the soft `classify_item_with_source_prior` (bounded additive prior; `classify_item`'s ungated ranking stays source-agnostic, lifted to the free `classify_item_ranking`). Cleanup: the dead `store::MENTIONS_EDGE`/`store::ENTITY_LABEL` duplicates removed (ingest owns `MENTIONS_ENTITY`). 17 commonplace tests green, clippy-clean. |
| `rustyred-thg-intake` | CommonPlace source intake (`SPEC-COMMONPLACE-SOURCE-INTAKE-AND-ROUTING`, Layer A + the C2 act seam): the ingestion-spoke framework, sibling to `rustyred-thg-connectors` (sync, tokio-free, generic over the `GraphStore` the `commonplace` store wraps; the connectors-vs-mcp split mirrored on the intake side, keeping `commonplace` a pure data layer). `SourceSpoke` maps a service's native records onto `IngestInput` (the universal capture target) and never calls back into the agent; `sync_source` is the driver (fetch the scoped delta page by page over a `RecordTransport` seam, map each record, ingest the page through the batch path, dedupe by source ref for A3 idempotency, advance + return the cursor in a `SyncReport`; a mapping error skips one record, not the sync). `SourceScope` (A2) is scoped-by-construction (containers/since_ms/max_records/filters); `FixtureTransport` is the in-memory recorded-response transport (tests + the universal path) that honors the scope filters and paginates with a resumable cursor. `MappedSpoke` (A5) is the universal path: a `SourceContract` (`FieldMap`/`Schema`/`Mcp`) drives the record->`IngestInput` mapping from data, so any describable source becomes a spoke with no bespoke Rust (all three variants share one `IngestFieldMap` shaping core and differ only in record discovery, the transport's job); `McpRecordTransport` is the live puller for the `Mcp` variant: it connects to a real MCP server through the connectors transport, `tools/call`s a read-tool, and parses its result into records (the spec's "resources or read tools", reusing existing connectors machinery, no new MCP protocol). Five curated spokes (A6): `GmailSpoke`/`GSuiteSpoke`/`OutlookSpoke`/`NotionSpoke`/`LinearSpoke` (Linear maps an issue to a `Task` with state/priority/due riding the scalars; each names auth/scope/mapping/cursor). All five spokes have live REST transports in `rest.rs` over one injected HTTP seam (`HttpRequest` + `ureq_fetch`): `GSuiteDriveTransport` (Drive `files.list`), `GmailHttpTransport` (messages list + per-id metadata get, body = snippet), `OutlookHttpTransport` (Graph `/messages`, `@odata.nextLink` cursor), `NotionHttpTransport` (database `query` POST + `Notion-Version`), `LinearHttpTransport` (GraphQL issues -> Tasks, raw-key auth). Each maps scope -> the service's query (folders/labels/teams; `since_ms` -> the service's time filter via an in-crate epoch-ms <-> RFC3339/ISO with no date dep), threads the service cursor, and normalizes the native response into the spoke's flat shape. The HTTP call is injected, so query-build + response-parse is fixture-tested and only a real token swap reaches the network (a `with_token` real path + one `#[ignore]` live smoke per source). Content bodies are not exported (metadata/snippet only); full-content fetch is a named follow-up. `act.rs` is the Layer C2 volume-absorber seam: a `NeedsYou` item carries an `AgentSuggestion`; `accept_suggestion`/`absorb_residue` fire it through the `ActSeam` trait and land the result on the object (the production `ConnectorActSeam` wraps `invoke_affordance` with `InvokePolicy::FireAllowlist`; tests use a recording mock). Path-deps: `commonplace` + `rustyred-thg-core` + (act seam) `rustyred-thg-connectors`/`rustyred-thg-affordances` + `ureq` (live REST). 20 intake tests green (4 unit + 16 acceptance) + 5 ignored live REST smokes, clippy-clean; fixture-only by default. |
| `design-check` | Static design-engineering checker and skill-pack payload for CSS, token, typography, motion, and accessibility receipts. |
| `ensemble` | Capability-pack registry, budgeted selector, and trust ladder over RustyRedCore-THG. It sits above tool-level affordance selection and emits replayable `EnsembleDecision` records for pack/agent/tool choice under budget. |
| `pilot-core` | Servo-free browser automation core: locator contracts, actionability and auto-wait, geometry snapshots, coordinate synthesis, and web-first assertions behind a `BrowserDriver` trait. It is intentionally substrate-free while logic migrates out of `rustyred-web`. |
| `prose-check` | Deterministic writing-engineering style checker and skill-pack payload for Theorem harness receipts. |
| `theorem-copresence` | Headless co-presence peer + surface-adapter seam (handoff `~/Downloads/handoff-copresence-substrate-peer.md`): the general interaction model as a peer over the substrate so any surface (or agent) co-edits rather than living inside one editor. `SubstratePeer` HLC-stamps `StructuredOp`s and converges **structure** on the graph CRDT (`crdt::join_delta`/`diff_since`) while also writing the real `ThgExecutor` command path (`GraphNodeUpsert`/`GraphEdgeUpsert`); **free text** lives in `yrs` 0.27 sequence-CRDT `TextRegion`s (browser-Yjs wire-compatible) exchanged by state-vector deltas; **awareness** rides the `working_log` (presence as a typed payload, read by `subscribe_after` cursor). `SurfaceAdapter` (`to_peer`/`from_peer`) splits into `InSubstrateAdapter` (built: `adapters::note` and W5 `adapters::code`) and `InstrumentAdapter` (Track A computer-use; boundary defined, not built). W5 adds `CursorPos::FilePosition` and `CodeSurfaceAdapter`: per-open-file code awareness with file:line:col presence, pending edit footprints as graph metadata, deterministic working-log cursor ordering, and explicit `GitMergeOnly` content strategy; it rejects text-region file-byte writes so source still merges through W2 git, not Yrs. Text regions persist as blobs via `DocTree::put_body`/`resolve_body` over `DiskObjectStore`, keyed by (scope, region_id). Two-peer convergence proof (concurrent structured + same-region text edits converge identically), presence exchange, and code-surface tests: 5 tests green, clippy-clean. Workspace member; path-dep into `rustyred-thg-core`. Codex-built, CC-verified. Scope note: persistence is process-scoped (the per-peer `DocTree` is in-memory, so a region does not rehydrate across a peer restart; durable cross-restart = serde the `DocTree` like E0.5, a named follow-up). |
| `theorem-harness-core` | Rust-native harness kernel: pure run state, transition executor, guard table, state hashing, replay/fork-ready event receipts, and toolgraph toolkit selection ported from Theseus `apps/orchestrate/runtime/`. Pure logic and parity-tested against Python reference corpora. |
| `theorem-harness-runtime` | GraphStore-backed harness runtime seam: persists `theorem-harness-core` transition receipts as `HarnessRun` / `HarnessEvent` graph nodes plus append-chain edges, keeping storage out of the parity kernel. Also holds `job_queue` (Dispatch v2 job-board persistence: submit/list/note/archive over any `GraphStore`). Tests cover memory and RedCore reopen persistence. |
| `theorem-harness` | SDK v2 Rust surface over `theorem-harness-core` and `theorem-harness-runtime`: run handles, sessions-as-scopes, idempotency tokens, cancellation, resumable events, replay, and trace export. This is the source surface for generated Python/Node/Swift/WASM bindings. |
| `theorem-acp` | CommonPlace ACP host for external coding agents: spawns ACP-capable subprocesses over newline-delimited JSON-RPC stdio, sends `initialize` with host-owned file/terminal capabilities, injects the Theorems-Harness V2 MCP server in `session/new`, stages file writes and PTY command runs for frontend approval cards, mirrors the receiver-sidecar path, and emits copresence agent presence for shared workspace rooms. The raw command PTY lives in the host only; CommonPlace renders structured ACP events as native thread UI. |
| `theorem-agentd` | Local assistant daemon: OpenAI-compatible model loop, schema-guarded MCP tool host, receiver sidecar, capture/relay loop, and compute-offload ledger. |
| `theorem-browser-agent` | Rust-native browser-use perceive/govern/afford kernel: `ContextCommandState`, `PerceptionBundle`, `ActionRailBundle`, playbook and `BrowsingRun` receipt contracts. Servo-free and graph-adjacent; low-level web I/O routes through `rustyred-web`. |
| `theorem-dispatch` | Postgres hot execution queue for Dispatch v2: sqlx-backed `dispatch_jobs`, atomic `FOR UPDATE SKIP LOCKED` claims, leases, retries/dead-letter handling, and state counts. The THG board remains canonical for coordination while Postgres owns hot execution state. |
| `theorem-receiver` | Dispatch v2 receiver (replaces the rejected GitHub-Actions dispatcher): an outbound-only launcher loop against the cloud harness that reads pending jobs, starts one exactly once with `job_note(start_session_ref)`, and spawns the locally-installed `claude` / `codex` CLI in a mapped worktree. Lane detection via PATH scan; `claude -p` / `codex exec`; `ANTHROPIC_API_KEY` stripped from the child env; child exit is appended as a `job_note` receipt. No engine state, no inbound port, no stored tokens. Heads are shaped as adapters behind one `HeadAdapter` trait (`head.rs`, the FDW-for-models pattern): a future head lands as config plus one impl (head_id/detect/spawn_cmd/intent_template/env_policy/receipt_parser), not a rewrite; trust tiers + selection priors live in the Ensemble registry, not a parallel one. The sandbox execution seam exposes `SandboxRuntime`; `OpenSandboxRuntime` is the sidecar backend and `LocalProcessSandbox` is the dev/no-sidecar backend over a temp worktree with `put_files`/`run`/`get_files`, deny-by-default env inheritance, and `run_streaming` support via `SandboxStreamEvent` plus `SandboxCancelToken` for stdout/stderr events, timeout exit events, execd `data:` event parsing, and cooperative cancellation. Builds as a lib (Option A embed) and a bin (Option B standalone). Plan: `docs/plans/dispatch-queue/dispatch-v2.md`. |
| `reconstruction-engine` | Generative reconstruction engine (in `crates/` but NOT a workspace member; built separately). |
| `scene-os-core` | Rust SceneOS director: `SceneAtom`/`SceneRelation`/`SceneScene` (same JSON contract as Index-API/Theseus-UI), trusted projection + chrome catalogs, and `compile_scene_package(SceneCompileInput) -> ScenePackageV2` (goal/shape classify -> projection select; `answer_type` hints force a projection, e.g. `force_graph`). serde-only leaf; selects layout, does not compute it. Consumed in-process by `apps/theorem-gateway` (SceneOS add-on) and by `apps/theorem-ios`. |
| `scene-os-web` | SceneOS renderer bundle (Lane B): `render_scene(&ScenePackageV2) -> String` embeds a self-contained canvas+d3 renderer (`web/dist/scene-os.bundle.js`, esbuild IIFE from `web/src/`) into `web/scene-host.html` and injects the package JSON via the `scene_payload_json` escape path (`<`,`>`,`&` -> `\uXXXX`; SERP injection pattern). The renderer runs a d3-force settle and a spawn->settle->annotate enter choreography (origin from package provenance; `prefers-reduced-motion` reveals settled directly); model-explanation atoms (`kind:"annotation"`) render as d3-annotation callouts pinned to anchor nodes. Served over HTTP by the gateway's `GET /scene/{id}`. |

## The Two Language Sides + the Bridge

- **Rust** (`rustyredcore_THG/` + `apps/browser*/`): the substrate engine, the browser, RustyWeb, native symbolic engines.
- **Python** (`apps/notebook/`, `apps/orchestrate/`): the mirror of Theseus's inference layer (reference engines, routing kernel, parity + cost gates, evolution callers).
- **Swift** (`apps/theorem-ios/`): the native phone client surface over ScenePackageV2. It currently builds as a Swift Package so Command Line Tools can validate the core before Xcode is available.
- **PyO3 bridge**: `rustyredcore_THG/src/lib.rs` is a `#[pymodule]` exported to Python as **`theseus_native`** (maturin, `pyproject.toml`). The Rust fn is named `rustyredcore_THG` but `#[pyo3(name = "theseus_native")]` overrides the Python-visible module name. Do NOT remove that name override: without it the symbol is `PyInit_rustyredcore_THG` and Python's `import theseus_native` fails silently into the slow Python fallback path.

## GraphStore: three stores, one trait

`rustyred-thg-core` defines the `GraphStore` trait. Three impls, used for different durability needs:

- **`InMemoryGraphStore`** - ephemeral, in-process. Tests + scratch.
- **`RedCoreGraphStore`** - durable, file-backed, in-process (AOF + snapshots). `open(data_dir, RedCoreOptions)`; `RedCoreOptions::default()` is `AofEverysec`, use `AofAlways` when you need fsync-per-commit determinism. **This is the "in-process substrate with no API boundary" the browser persists to.** It implements `GraphStore` (writes delegate to the inherent AOF-backed durable upserts; reads serve from an in-memory mirror that `recover()` rebuilds from the AOF on open). TTL methods keep the trait defaults (durable TTL is a follow-up; delegating TTL writes to the mirror would skip the AOF).
- **`RedisGraphStore`** - connects to a Redis/RustyRed server (an API boundary, out-of-process).

`RedCoreGraphStore` and `RedisGraphStore` also expose inherent `get_node`/`upsert_node` etc. that can **shadow** the trait methods. When you need the trait method on those types, call it via UFCS: `GraphStore::get_node(&store, id)`.

## The substrate-native browser

- **`apps/browser/`** embeds Servo as a Cargo **git dependency** pinned to a known-good rev (NOT a fork). It builds `libservo` via plain `cargo` after `./mach bootstrap` provides system libs (the external-embed path, like Verso). The embedder constructs `ServoBuilder::default().event_loop_waker(...).build()`, a headless `SoftwareRenderingContext`, and a `WebView` via `WebViewBuilder::new(&servo, rendering_context)`.
- **Substrate seam 1 (DOM -> graph state)**: `WebViewDelegate::load_web_resource(&self, webview, WebResourceLoad)` (NOT `ServoDelegate`, which fires only for non-WebView loads). On `notify_load_status_changed(LoadStatus::Complete)`, build a `LoadedPage` and call `theorem_browser_substrate::ingest_loaded_pages` / `durable_browser_session_with_options`, which writes the page into a `GraphStore` (use `RedCoreGraphStore` for persistence).
- **`apps/browser-substrate/`** is the Servo-free seam: `LoadedPage -> rustyred-web (build_v2_fixture_crawl) -> CrawlGraph::apply_to_store`. Keep it Servo-free so it stays cheap to iterate.

## Build & Dev Commands

There is **no root Cargo workspace**. Pick the right workspace/crate:

```bash
# RustyRed/THG core workspace (the engine + bridge)
cd rustyredcore_THG && cargo check --workspace # type-check the workspace; use maturin for PyO3
cd rustyredcore_THG && cargo test -p rustyred-thg-core   # test the core
cd rustyredcore_THG && cargo test --no-run -p rustyred-thg-core  # compile-only coherence check
cd rustyredcore_THG && cargo test -p theorem-harness-core # test the Rust-native harness kernel
cd rustyredcore_THG && cargo test -p theorem-harness-runtime # test GraphStore-backed harness event persistence
cd rustyredcore_THG && cargo test -p theorem-browser-agent # test browser-use policy/perception/action contracts
cd rustyredcore_THG && cargo test -p theorem-copresence # test the headless co-presence peer seam
cd rustyredcore_THG && cargo test -p rustyred-web # test crawl/search plus browser-use web primitives

# The seam crate (Servo-free, fast: no libservo)
cargo test --manifest-path apps/browser-substrate/Cargo.toml

# The gRPC search server (standalone Cargo root, NOT a rustyredcore_THG member)
cd apps/theorem-grpc && cargo build -p theorem-grpc   # binds [::]:$PORT (default 50071 local); /health and /ready share the gRPC listener

# The harness HTTP transport (standalone Cargo root, NOT a rustyredcore_THG member)
cd apps/theorem-harness-server && cargo test
cd apps/theorem-harness-server && PORT=50080 THEOREM_HARNESS_DATA_DIR=harness-data cargo run

# The native iOS client scaffold (CLT-friendly; full simulator/archive needs Xcode)
swift build --package-path apps/theorem-ios
swift run --package-path apps/theorem-ios TheoremIOSSmoke

# The Servo embedder builds in CI only (heavy: ~30 min libservo from cold).
# Trigger: gh workflow run servo-browser.yml --ref main
#   - servo pinned in apps/browser/Cargo.toml (rev) + apps/browser/rust-toolchain.toml (1.95.0)
#   - the workflow reclaims ~20-25GB of preinstalled SDKs + sets CARGO_PROFILE_DEV_DEBUG=0

# PyO3 wheel (theseus_native) via maturin, from rustyredcore_THG
cd rustyredcore_THG && maturin develop        # build + install the wheel into the active venv

# Python parity / cost gates (mirror of Theseus inference)
#   live under apps/notebook/benchmarks/ and apps/notebook/inference_engines/.../tests/
```

## Conventions & Gotchas

- **Mirror discipline**: this is the projection, not the canon. Keep parity receipts + the PyO3 surface reconciled with Theseus. Surface drift, don't bury it.
- **No root workspace**: `-p <crate>` only works inside the relevant workspace (e.g. `cd rustyredcore_THG`); `apps/browser*` are standalone crates reached via `--manifest-path`. Path deps cross from `apps/` into `rustyredcore_THG/crates/`.
- **Byte-parity discipline**: native Rust symbolic engines must byte-match the Python reference receipts. The differential gates live in `apps/notebook/benchmarks/`. A native port is not done until its parity gate is green over the reference set.
- **Servo build is the long pole**: it is CI-only and heavy. Pin the `servo` rev deliberately; bump on evidence. The embedder's `rust-toolchain.toml` must match Servo's (the whole graph builds with one toolchain). `mach` needs its Python deps (`toml`) in the invoking interpreter before its venv activates.
- **Editing a file another agent (Codex) is on**: Codex is frequently active in this repo. Commit only with an explicit pathspec (`git commit -- <paths>`), never a bare `git commit` (the shared index can carry another agent's staged files). Claim a seam before overlapping. The harness `coordinate` endpoint has been returning 503 (down); use git history + commit messages as the coordination channel meanwhile.
- **No emojis. No em/en dashes** (match the user's style across repos: use colons, parens, semicolons).
- **No time/effort estimates** in plans or reports.
- **Dependencies named in a spec are information for you, not gates.** Check the tree, decide, and note your reasoning on the job.

## Status / Current Direction

Per-strand status + current direction moved to [docs/reference/status-current-direction.md](docs/reference/status-current-direction.md) to keep this file a lean navigation map. Update that file (not this one) when a strand's status changes; keep only the repo map + conventions here.
