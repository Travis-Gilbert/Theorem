# Theorem

Theorem is the **Rust-native substrate spine**: the programmable Rust projection that mirrors the Theseus / Index-API workspace. It carries the RustyRed/THG substrate engine, the PyO3 bridge and parity surfaces that exercise it, the substrate-native browser, the RustyWeb crawler/search engine, and the symbolic-engine plans.

This file is the navigation truth for the repo. Read it first. Order of truth: this map, then the actual code, then the plan docs (plans lag code). If a session changes architecture (new crate, renamed module, removed subsystem, new entrypoint), update this file before ending the session.

## The Mirror Rule (read this before touching anything)

- **Theseus is canonical.** Theseus = the Python + Memgraph + Postgres application/runtime (separate repo: `github.com/Travis-Gilbert/Theseus`, the local `Index-API` checkout). Theorem **mirrors** the Rust projection and its bridge contracts so Rust-side work can move at its own cadence while staying auditable against the source workspace.
- **Do not let Theorem and Theseus diverge silently.** The Python files here (`apps/notebook/`, `apps/orchestrate/`) are mirrors of their Theseus counterparts. Changes that affect the contract (parity receipts, affordance gates, the PyO3 surface) must stay reconciled with Theseus.
- Last sync recorded in `README.md` (2026-06-20; added `apps/harness-console`, the Theorems Harness web console: a greenfield Next.js 16 + React 19 + Tailwind front door over the harness MCP/HTTP backend (one typed `HarnessClient`, mock + live impls), with ten authenticated surfaces + Browser-Use-style onboarding, the 4x4 design-math token file, the DotGrid depth system, the Dynamic Island spine (omnibar + TOC + RustyWeb search + Cmd-K), cosmos.gl Lane A graph, and a real Yjs collaborative agent IDE; `next build` green). Prior 2026-06-19 sync added `crates/commonplace` + `apps/commonplace-api`, `apps/rustyred-embedded`, and `crates/theorem-copresence`. Bump it when you re-sync.
- Open question still live in `Theseus/Theorem.md`: whether a good tool built on the Theorem (Rust) layer becomes a **promotion candidate** to the canonical Theseus layer. Treat Rust-side tools as candidates, not canon.

## Repository Layout

| Path | What it is |
|------|------------|
| `rustyredcore_THG/` | The RustyRed / THG Rust workspace **and** the PyO3 bridge. A real Cargo workspace (`Cargo.toml [workspace]`). Root lib (`src/`) is the PyO3 module exported to Python as `theseus_native` (maturin). Has its own `Dockerfile` + `railway.toml`. |
| `rustyredcore_THG/crates/` | The graph-engine crates (see table below). |
| `apps/browser/` | `theorem-browser`: the Servo-embedded substrate-native browser. Standalone Cargo crate (NOT in the rustyredcore_THG workspace). |
| `apps/browser-substrate/` | `theorem-browser-substrate`: the Servo-free page->substrate seam. Standalone crate; depends only on `rustyred-web` + `rustyred-thg-core` (no Servo), so it builds + tests in seconds. |
| `apps/rustyred-embedded/` | `rustyred-embedded`: North Star E0 embedded mode (E0.1 linkable core + E0.2 restart rehydration landed). Link the crate and run RustyRed in-process over a local directory, no server: `Engine::open(dir, EmbeddedConfig)` owns a durable `RedCoreGraphStore` behind the in-process `SharedStore` seam (`rustyred-thg-mcp`) and exposes the full typed GraphQL surface as local `query`/`mutate`/`introspect` calls (synchronous, no socket, no async runtime). Restart rehydration rides `RedCoreGraphStore::open`->`recover`, proven through the agent surface. E0.3 adds the single stdio binary (`src/main.rs`: opens an Engine over a data dir, serves MCP JSON-RPC over stdin/stdout, no socket; `Engine::handle(request)` drives the full MCP surface) + `tests/stdio.rs`. No server framework in the tree (no axum/tonic; transitive reqwest/hyper is an HTTP client via rerank/harness-runtime -- feature-gating for a minimal binary is a named follow-up). E0.4 adds the TOML config (`EmbeddedConfig::from_toml_str`/`load_for_dir`; theorem.toml lives in the data dir, configures tenant/durability/graphql_default_surface; data_dir is the binary arg/env). E1 (stream transport) confirmed: stream_publish->stream_read round-trips through `Engine::handle` in-process (local live-tail, no server). E0.5 folder tree: the Engine owns a serde-persisted `DocTree` + `DiskObjectStore`; `fs_write` (content hash + inline/overflow + a durable `file:{path}` graph node), `fs_read` (point lookup), `fs_ls` (prefix scan) -- the folder tree IS the agent's filesystem, durable across restart. (E0.5 was NOT blocked on B3: DocTree is serde-able + exported; B3's DocTree-as-ColdIndex gap is a separate store concern.) E0.5 fully complete: a written file ALSO gains a deterministic offline embedding (designated File/embedding vector property), so it is vector-searchable. **Area E COMPLETE: E0.1-E0.5 + E1, 11 tests green, clippy-clean.** Standalone Cargo root (bare `[workspace]`), path-deps into `rustyred-thg-{mcp,core}`. Spec: `docs/plans/rustyred-multimodel/E0-embedded-mode.md`; loop status + North Star scorecard + gated remainder: `docs/plans/rustyred-multimodel/LOOP-STATUS.md`. |
| `apps/theorem-ios/` | SwiftPM scaffold for the native Theorem iOS client. Holds the SwiftUI shell, Dynamic Island control surface, ScenePackageV2 models, mobile projection switcher, local smoke executable, and the pre-Xcode compile path. Full simulator/archive work still needs Xcode. |
| `apps/theorem-harness-server/` | Standalone Axum JSON/HTTP transport over `theorem-harness-runtime` and `RedCoreGraphStore`. Serves run list/detail plus room status, presence, live intents, actor mentions, and room records for iOS/web harness surfaces. Own Cargo root, not a `rustyredcore_THG` member. |
| `apps/theorem-grpc/` | `theorem-grpc`: Theorem's first gRPC server. Serves `theseus_search.v1.SearchService` in pure Rust over the RustyRed substrate (`rustyred-web` search + `rustyred-thg-core` store/PPR). No PyO3, no Django. Standalone Cargo root (NOT a `rustyredcore_THG` member); own `Dockerfile` + `railway.toml` (its own Railway service). URL-swap target for civic-atlas-server's `SearchServiceClient` via `THEOREM_SEARCH_URL`. Binds `0.0.0.0:$PORT` (default `50071` local). |
| `apps/theorem-gateway/` | `theorem-gateway`: browser-facing GraphQL gateway (Axum + async-graphql + tonic) whose resolvers call `theorem-grpc` (`SearchService` + `CodeCrawlerService`) and the GL-Fusion model endpoint, shaping results for the web. Stores nothing: a front door + translator mirroring `our-civic-atlas-backend`. Generates tonic CLIENT stubs from vendored protos (`theseus_search/v1`, `theorem_code/v1`); path-deps into `rustyredcore_THG` only for the SceneOS add-on (`scene-os-core` compile + `scene-os-web` serve), so the Dockerfile copies the whole crates dir. Standalone Cargo root; own `Dockerfile` + `railway.toml` (its own Railway service, public domain). `POST /graphql` + GraphiQL on `GET /graphql` + `/healthz`; binds `0.0.0.0:$PORT` (default `50080`). Per-IP token-bucket rate limiting (in-memory or Valkey) + a public ingest allowlist guard the browser boundary. Showpiece `askAgent` returns the GL-Fusion answer next to the graph context that fed it. SceneOS add-on: `sceneForInput` compiles a force-graph `ScenePackageV2` (community/centrality/annotation atoms) via `scene-os-core`; `GET /scene/{id}` serves it as one self-contained HTML asset via `scene-os-web` (explosion->settle->annotate). `schema.graphql` committed for frontend codegen. |
| `apps/ios/TheoremKit/` | Swift Package (`TheoremKit`), distinct from `apps/theorem-ios`. Shared kit layer. |
| `apps/commonplace-api/` | `commonplace-api`: the CommonPlace interoperability API seam (consumer-loop plan unit F3). A typed consumer GraphQL profile over the `commonplace` object model (Query: item/items/collection/collections/collectionItems/search; Mutation: ingest/putNote/editItem/createCollection/addToCollection), gated by per-instance API keys (`x-api-key` header -> `ApiKeyToken` request data -> `ApiKeyRegistry`; every resolver authorizes before any store access, invalid key rejected). Axum + async-graphql 7 (axum 0.8, matching async-graphql-axum); `POST /graphql` + GraphiQL on `GET /graphql` + `/healthz`; binds `0.0.0.0:$PORT` (default 50090). Standalone Cargo root (bare `[workspace]`, like `apps/theorem-grpc`); path-deps into `rustyredcore_THG/crates/{commonplace,rustyred-thg-core}` and consumes ONLY commonplace's public API (zero-collision lane). Backing is the in-memory store for this slice (each instance = its own dataset); the durable `RedCoreGraphStore`+`DiskObjectStore` swap behind the identical schema is the named follow-up. 3 acceptance tests (read/write with a key; invalid+missing key rejected; separate instances = separate data) + live HTTP smoke (authed ingest auto-structures over HTTP, unauthed rejected); clippy-clean. **I1 (ask-over-store)** added: `src/retrieve.rs` + an `ask(question, k)` query that runs unified retrieval over the consumer store fusing vector (the engine embedding index), lexical (in-crate idf token-overlap = the FTS arm), and graph (`SIMILAR_TO` propagation) via reciprocal-rank fusion, returning per-item provenance (which arms surfaced each) + an answer from an `AnswerModel` seam (`NoModel` -> honest extractive answer; generative GL-Fusion/RunPod is the follow-up). live HTTP `ask` smoke. **I2 (proactive briefing)**: `src/briefing.rs` + a `briefing` query (recent by `updated_at` / newly-connected by `SIMILAR_TO` degree / open-threads = open-tagged or unfiled). **I4 (discovery)**: `src/discover.rs` + a `discover` query (vector-similar item pairs not yet `SIMILAR_TO`-linked, ranked; excludes connected pairs). **AG3 (MCP access)**: `src/mcp.rs` + a second `commonplace-mcp` stdio binary speaking newline-delimited JSON-RPC 2.0 (`initialize`/`tools/list`/`tools/call`) exposing put_note/ingest/get_item/list_items/search, so any model reads/writes items over MCP. **X2 (import/export)**: `src/portability.rs` + an `export(format: JSON|MARKDOWN)` query (lossless JSON / human-readable markdown) and an `importItems(data)` mutation that recreates items + collections with their original ids so memberships survive a round-trip. 10 api tests (3 F3 + 2 I1 + 1 I2 + 1 I4 + 1 AG3 + 2 X2) + live HTTP smokes + an MCP-stdio acceptance; clippy-clean. Follow-ups: native FTS (core `FullTextIndex`/tantivy) over the lexical arm, personalized-PageRank over the discovery/briefing graph arm, durable RedCore+Disk backing for both bins, GL-Fusion/RunPod generative answers + real embeddings (the `AnswerModel` + `Embedder` seams are in place), Railway deploy. |
| `apps/obsidian-sync/` | `theorem-harness-sync`: device-side Obsidian community plugin (TypeScript, esbuild). Mirrors the tenant's memory docs into a vault (Phase 1 pull) and writes note edits + `[[wikilinks]]` back into the graph (Phase 2 write-back). Talks to the harness over `requestUrl`: `GET /v1/tenants/:tenant/memory/docs` for read and the `upsert_note` MCP tool over `POST /mcp` for write. Three echo guards (content-hash gate, remote-write suppression, conflict copies). Standalone npm package, not a Cargo member. Plan: `docs/plans/obsidian-sync/`. |
| `apps/harness-console/` | `harness-console`: the **Theorems Harness web console** (Next.js 16 App Router + React 19 + **Tailwind v4 CSS-first** + TypeScript; ESLint 9 flat config since Next 16 removed `next lint`), target `harness.theoremsweb.com`. Greenfield (does NOT inherit Context-Theorem-UI); the only shared substrate is the harness backend, consumed over MCP/HTTP via one typed client (`src/lib/harness`: a `HarnessClient` interface with a deterministic `mock` impl + a `live` MCP/GraphQL impl, env-selected by `NEXT_PUBLIC_HARNESS_SOURCE`). Built per `~/Downloads/SPEC-harness-console-surface.md` + `SPEC-harness-console-libraries-and-design-math (4).md`. Rail IA: top-level **Canvas** + **Inbox**, then an **Agents** group (Agent/Memory/Skills/Rooms/Runs), then Account (API Keys/Providers/Usage/Connections/Settings); `/` redirects to `/canvas`. Twelve authenticated surfaces under `app/(console)/`: **Canvas** (the persistent spatial home: a `@xyflow/react` board whose arrangement + viewport persist to localStorage in the **JSON Canvas** format `jsoncanvas.org`, restored exactly on reopen; durable harness-graph backing is the follow-up); **Inbox** (mail-style three-pane over harness mentions/runs/system + a Dispatch-v2 **task board**); Agent (composed `agent:theorem` thread + per-head provider-key status + alignment verdict); Memory (list landing + cosmos.gl graph + cluster + markdown editor + dropzone, archive/trash); Skills (content-addressed pack lifecycle + Mistral-style editor + collaborative agent IDE); Rooms; Runs + replay; API Keys + per-client install snippets; Providers w/ `credential_ref`; Usage; Connections + MCP Hub; Settings; plus `app/(onboarding)/claim` (Browser-Use-style agent self-register + GitHub claim). Design system in one token file (`src/app/globals.css`): the 4x4 math (Space/Type/Color/Hierarchy), the 3-layer depth system (`DotGrid` ambient field, `.material` surface, 4-level elevation scale), palette white/grey/black-hairline/oxblood `#8A2E29`/green `#5f7d4f`. **The shadcn token bridge**: `@theme inline` maps the standard shadcn semantic names (`--color-primary`/`--color-muted-foreground`/`--color-border`/...) onto the palette vars, so third-party shadcn/21st.dev registry components inherit the oxblood/grey palette automatically (the enabling layer for the planned file-tree / file-upload / ai-input / ai-planning component adoption); the one `muted` clash (our "text" vs shadcn's "surface") is resolved by adopting shadcn's split (`text-muted-foreground`). The **Dynamic Island** (`src/components/island`) unifies omnibar + scroll-spy TOC + RustyWeb search + Cmd-K command palette. Renderers: cosmos.gl (Lane A, real `@cosmos.gl/graph@3` GPU graph) + D3 (Lane B); editors CodeMirror 6; the collaborative IDE is real Yjs + `y-codemirror.next` (two synced docs + relayed awareness so a harnessed agent shows as a live attributed remote cursor). Standalone npm package (own `package.json`, not a Cargo member); `next build` green (17 routes), surfaces verified in-browser (incl. Canvas persistence round-trip + the bridge resolving). README + `.env.example` in the crate. |
| `apps/jobintel/` | `jobintel`: a standalone job-intelligence + outreach CLI that is a light HTTP *consumer* of a running RustyRed (no path-deps into the substrate; talks only to the public tenant routes). 0.1: ingests HN "Who is Hiring" + Greenhouse/Lever/Ashby ATS into the graph (Company/Role/Skill/Source/Person nodes + edges), designates Role/Profile embeddings as HNSW, ranks roles against a profile by blending vector search + PPR/PageRank + flag boosts, and drafts per-lead context packs (`context/pack`) into `out/queue.md`. 0.2 (`src/outreach/`): an outreach state machine on the Role graph + append-only `OutreachEvent` trail, a Gmail create-draft bridge (writes drafts, never sends; second HTTP seam at `gmail.googleapis.com`, base-URL-swappable for the mock), a day-4/day-9 follow-up cadence with a daily cap, reply detection + `drafted->sent` advancement via `sync`, and a per-template/per-lead-type learning signal (`OutcomeRecord` + `outreach stats`). Standalone Cargo root (bare `[workspace]`), `reqwest::blocking` + clap + chrono + base64. `Embedder` seam has `hash` (offline default), `http` (Theseus SBERT swap), and `bge` (candle bge-small-en-v1.5, feature-gated). Specs: `~/Downloads/jobintel-build-spec.md` (0.1), `~/Downloads/jobintel-0.2-spec.md` (0.2). |
| `apps/notebook/` | **Python** mirror of Theseus's inference layer: `inference_engines/` (symbolic-engine contracts, Python reference engines, native bridge adapters, Gate-0 affordance coverage, tests), `inference_kernel/` (native-vs-Python routing/execution), `benchmarks/` (byte-parity + cost gates), `discovery_runs/` (Rust-theorem callers routing archive/policy-evolution through the native evolution engine). |
| `apps/orchestrate/runtime/` | **Python**: `map_elites_tick.py`, the orchestration tick for native MAP-Elites archive throughput. |
| `Theseus/Theorem.md` | Source framing: Theseus-as-canonical / Theorem-as-Rust-projection, and the promotion-candidate idea. The "why this repo exists" doc. |
| `docs/plans/` | The plan tree (see Status). `rust-theorem-symbolic-engines/`, `rusty-red-web/`, `rustyweb-v1-design/`, `commonplace-substrate-reconciliation/`. |
| `docs/reference/` | Source architecture context, incl. `commonplace-substrate-architecture-part-4-1.md` (symbolic compute offload). |
| `.github/workflows/servo-browser.yml` | CI build for the Servo embedder (heavy; manual trigger). |

### RustyRed/THG crates (`rustyredcore_THG/crates/`)

| Crate | Purpose |
|-------|---------|
| `rustyred-thg-core` | The graph engine core: `GraphStore` trait, the store impls, mutations/transactions, indexes, TTL, AOF durability, symbolic engines (`symbolic.rs`), graph-level mutation hooks (`hooks.rs`: post-commit reactive compute), and the native relational core (`access_method.rs`, `relational.rs`, `planner.rs`, `cold_fragments.rs`, `working_log.rs`). The heart of the substrate. Storage spine (cut 6) lives here too: `object_store.rs` (the zstd-compressed disk-backed content-addressed cold object/document store, the cold tail's durable home), `doc_tree.rs` (copy-on-write B-tree cold document namespace with inline leaves and overflow blobs), `cold_index.rs` (the `ColdIndex` catalog trait + in-memory/disk/ordered-native impls: id -> tier/object-hash keyed rehydration), `ordered.rs::EvictionFrontier` (per-scope ordered index over `last_accessed_ms` for O(k log n) coldest-first eviction), and version-neutral `evict_node`/`readmit_node`/`evict_edge`/`readmit_edge` on `GraphStore` (residency changes that never bump `stats().version`, so the PPR cache stays warm). |
| `rustyred-web` | RustyWeb. V0 fixture crawler kernel + V2 hardening: URL canonicalization, `a[href]` extraction, BLAKE3 content snapshots, page->graph emission (`build_fixture_crawl_graph` / `build_v2_fixture_crawl`), application into a `GraphStore`, plus the browser-use web primitives (`PageState`, `BrowserAction`, `web_consume_to_graph`) under `browser_engine.rs`. |
| `rustyred-hipporag` | HippoRAG 2 candidate generation for RustyRed: `Page`/`Phrase`/`Hub` schema, phrase indexing, RAPTOR-style summary hubs via graph hooks, query-specific PPR retrieval, and membrane-compatible `Candidate` output. Reranking/admission stay in `rustyred-rerank` and `rustyred-membrane`. |
| `rustyred-membrane` | Shared context admission and eviction membrane for RustyRed graph-backed context. Used by HippoRAG, RustyWeb, and fractal expansion to admit candidates before they enter the graph-backed context surface. |
| `rustyred-rerank` | Reranker scorer implementations for RustyRed membrane admission, including the lexical cross-encoder seam consumed by search/fractal pipelines. |
| `rustyred-thg-adapters` | Adapters into the core. |
| `rustyred-thg-affordances` | Connector-as-substrate learning registry: MCP connector tools become first-class `Affordance` graph nodes; the substrate learns which affordance to reach for from accumulated invocation outcomes (PPR over `SERVED_TASK`/`PRODUCED_OUTCOME` edges plus fitness), scoped per agent by a `CapabilityScope`. Structural sibling of `rustyred-thg-adapters`; reuses `theorem-harness-core` for the pairformer A/B validation gate. 15 tests green. |
| `rustyred-thg-code` | Code parsing plugin/runtime for tenant graph stores: parses source into code graph records so CodeCrawler and code-KG surfaces can search, explain, and explore repositories. |
| `rustyred-thg-connectors` | Live MCP connector transport: the outbound mirror of `rustyred-thg-mcp`. Connects to an external MCP server over stdio, performs the handshake, walks `tools/list`, and registers each tool as a learnable `Affordance` node via `rustyred-thg-affordances::register_connector`. Pure JSON-RPC protocol layer + thin `BufRead`/`Write` stdio transport (tokio-free, in-memory testable) + `connect_and_register` bridge. Slice 2 adds the invoke bridge (`invoke.rs`): `tools/call` -> `record_invocation` gated by `InvokePolicy` (dry-run default, `FireAllowlist` opt-in), with the `ConnectionTarget` persisted on the `Connector` node so a selected tool is reachable again. Closes the affordances plan's "Live MCP tools/list ingestion" seam. 19 tests green. Plan: `docs/plans/mcp-learning-layer/connector-transport-plan.md`. |
| `rustyred-thg-compat-server` | Standalone legacy HTTP control server for THG-Core command execution, run lookup, state hash, health, and readiness routes over memory or Redis-backed executors. |
| `rustyred-thg-geotemporal` | Geo + temporal indexing, including the plugin wrapper that registers the planner-facing `time_series` access method for `TimeRange` predicates. |
| `rustyred-thg-fractal` | Native Rust fractal expansion pipeline over RustyRed and RustyWeb. It grows corpus context by crawling/admitting web graph state into a lower-trust quarantined tier rather than doing graph-only retrieval. |
| `rustyred-thg-mcp` | Native Rust MCP server (graph reads/algorithms without a Python process in the loop). |
| `rustyred-thg-memory` | Graph-native memory: PPR-seeded `recall`, `consolidate`, `decay`, validity/contradiction, and the project-scope bias. Cut 6 adds the storage-spine eviction tier here: `ColdTier` (bundles a `ColdObjectStore` + `ColdIndex` + `EvictionFrontier`), `evict_decayed` (flag -> commit content object -> record cold index -> `evict_node` -> drop from frontier; reads the coldest tail from the frontier in O(k log n), never the O(n) memory-node scan; version-neutral), `rehydrate` (keyed cold-index lookup -> object -> `readmit_node`, the recall-triggered path), `recall_with_cold_tier` (rehydrate absent seeds + zadd-on-access), and `park_scope`/`unpark_scope` (warm tier: compile a scope to a `CompiledGraphPack`, evict it, rehydrate the whole subgraph via `checkout_graph_version`). Acceptance 1-5 proven in `tests/storage_spine_acceptance.rs`. |
| `rustyred-thg-pg-server` | Postgres wire protocol server for RustyRedCore-THG native views: trust-mode startup/auth, simple query plus Parse/Bind/Describe/Execute/Sync, typed row encoding, executor-backed `nodes` view queries, and SQL-to-`QueryIr` lowering for planner-backed joins. |
| `rustyred-thg-resp-server` | RESP protocol server for ordered-index commands, exposing a Redis-shaped command loop over `OrderedIndexRegistry` for focused protocol compatibility. |
| `rustyred-thg-server` | Product HTTP/gRPC/MCP surface over RustyRedCore-THG: tenant graph routes, query/Cypher APIs, harness coordination, browser actions, fractal expansion, and TTL sweeping. |
| `rustyred-thg-catalog` | Compatibility sqlx/Postgres catalog for tenants, projects, billing, auth, plus the legacy `cold_index`/`cold_scope` rows. The hot native path now lives in `rustyred-thg-core` via `OrderedColdIndex`, `NativeCatalog`, and the relational planner; keep `PostgresCatalog`/`PostgresColdIndex` for deployments that still need the old external catalog bridge. Runtime sqlx queries (no compile-time `DATABASE_URL`); the crate builds with no live DB, live round-trip is an `#[ignore]` test. |
| `commonplace` | CommonPlace consumer data layer (plan `docs/plans/commonplace-consumer-loop/`). The personal-database object model graph-native over `GraphStore`: `Item` (universal unit; any `ItemKind`, `Residency` field the sync layer reads, inline/blob `ItemBody`), `Collection` (`Collection` node + `IN_COLLECTION` membership edges, enumerable by reverse traversal), `Tag` (`Tag` node keyed by stable slug + `HAS_TAG` edge). The `Commonplace<S: GraphStore, B: BlobStore>` facade maps the model onto graph records; a `File`'s bytes live content-addressed in a `BlobStore` (`InMemoryBlobStore` for tests, the core `DiskObjectStore` for durability; both yield identical `sha256:<hex>` addresses). F1 = 6 tests green (in-memory + RedCore restart rehydration), clippy-clean. F2 = `IngestPipeline` with deterministic local `Embedder` seam: detects note/doc/link/image input, embeds it, classifies/creates auto collections from label embeddings, stores blobs, writes vector-searchable top-level item `embedding`, files folder-path metadata, writes `SIMILAR_TO` edges, and resolves near-duplicate `Entity` nodes. Divergence from the plan's "module over `rustyred-thg-catalog`": graph-native instead, so Items are first-class for F2 classification/I1 retrieve and acceptance stays DB-free; catalog stays the home for tenant/key/billing (F3). |
| `design-check` | Static design-engineering checker and skill-pack payload for CSS, token, typography, motion, and accessibility receipts. |
| `ensemble` | Capability-pack registry, budgeted selector, and trust ladder over RustyRedCore-THG. It sits above tool-level affordance selection and emits replayable `EnsembleDecision` records for pack/agent/tool choice under budget. |
| `pilot-core` | Servo-free browser automation core: locator contracts, actionability and auto-wait, geometry snapshots, coordinate synthesis, and web-first assertions behind a `BrowserDriver` trait. It is intentionally substrate-free while logic migrates out of `rustyred-web`. |
| `prose-check` | Deterministic writing-engineering style checker and skill-pack payload for Theorem harness receipts. |
| `theorem-copresence` | Headless co-presence peer + surface-adapter seam (handoff `~/Downloads/handoff-copresence-substrate-peer.md`): the general interaction model as a peer over the substrate so any surface (or agent) co-edits rather than living inside one editor. `SubstratePeer` HLC-stamps `StructuredOp`s and converges **structure** on the graph CRDT (`crdt::join_delta`/`diff_since`) while also writing the real `ThgExecutor` command path (`GraphNodeUpsert`/`GraphEdgeUpsert`); **free text** lives in `yrs` 0.27 sequence-CRDT `TextRegion`s (browser-Yjs wire-compatible) exchanged by state-vector deltas; **awareness** rides the `working_log` (presence as a typed payload, read by `subscribe_after` cursor). `SurfaceAdapter` (`to_peer`/`from_peer`) splits into `InSubstrateAdapter` (built: `adapters::note`) and `InstrumentAdapter` (Track A computer-use; boundary defined, not built). Text regions persist as blobs via `DocTree::put_body`/`resolve_body` over `DiskObjectStore`, keyed by (scope, region_id). Two-peer convergence proof (concurrent structured + same-region text edits converge identically) + presence exchange: 2 tests green, clippy-clean. Workspace member; path-dep into `rustyred-thg-core`. Codex-built, CC-verified. Scope note: persistence is process-scoped (the per-peer `DocTree` is in-memory, so a region does not rehydrate across a peer restart; durable cross-restart = serde the `DocTree` like E0.5, a named follow-up). |
| `theorem-harness-core` | Rust-native harness kernel: pure run state, transition executor, guard table, state hashing, replay/fork-ready event receipts, and toolgraph toolkit selection ported from Theseus `apps/orchestrate/runtime/`. Pure logic and parity-tested against Python reference corpora. |
| `theorem-harness-runtime` | GraphStore-backed harness runtime seam: persists `theorem-harness-core` transition receipts as `HarnessRun` / `HarnessEvent` graph nodes plus append-chain edges, keeping storage out of the parity kernel. Also holds `job_queue` (Dispatch v2 job-board persistence: submit/list/note/archive over any `GraphStore`). Tests cover memory and RedCore reopen persistence. |
| `theorem-harness` | SDK v2 Rust surface over `theorem-harness-core` and `theorem-harness-runtime`: run handles, sessions-as-scopes, idempotency tokens, cancellation, resumable events, replay, and trace export. This is the source surface for generated Python/Node/Swift/WASM bindings. |
| `theorem-agentd` | Local assistant daemon: OpenAI-compatible model loop, schema-guarded MCP tool host, receiver sidecar, capture/relay loop, and compute-offload ledger. |
| `theorem-browser-agent` | Rust-native browser-use perceive/govern/afford kernel: `ContextCommandState`, `PerceptionBundle`, `ActionRailBundle`, playbook and `BrowsingRun` receipt contracts. Servo-free and graph-adjacent; low-level web I/O routes through `rustyred-web`. |
| `theorem-dispatch` | Postgres hot execution queue for Dispatch v2: sqlx-backed `dispatch_jobs`, atomic `FOR UPDATE SKIP LOCKED` claims, leases, retries/dead-letter handling, and state counts. The THG board remains canonical for coordination while Postgres owns hot execution state. |
| `theorem-receiver` | Dispatch v2 receiver (replaces the rejected GitHub-Actions dispatcher): an outbound-only launcher loop against the cloud harness that reads pending jobs, starts one exactly once with `job_note(start_session_ref)`, and spawns the locally-installed `claude` / `codex` CLI in a mapped worktree. Lane detection via PATH scan; `claude -p` / `codex exec`; `ANTHROPIC_API_KEY` stripped from the child env; child exit is appended as a `job_note` receipt. No engine state, no inbound port, no stored tokens. Heads are shaped as adapters behind one `HeadAdapter` trait (`head.rs`, the FDW-for-models pattern): a future head lands as config plus one impl (head_id/detect/spawn_cmd/intent_template/env_policy/receipt_parser), not a rewrite; trust tiers + selection priors live in the Ensemble registry, not a parallel one. Builds as a lib (Option A embed) and a bin (Option B standalone). Plan: `docs/plans/dispatch-queue/dispatch-v2.md`. |
| `reconstruction-engine` | Generative reconstruction engine (in `crates/` but NOT a workspace member; built separately). |
| `scene-os-core` | Rust SceneOS director: `SceneAtom`/`SceneRelation`/`SceneScene` (same JSON contract as Index-API/Theseus-UI), trusted projection + chrome catalogs, and `compile_scene_package(SceneCompileInput) -> ScenePackageV2` (goal/shape classify -> projection select; `answer_type` hints force a projection, e.g. `force_graph`). serde-only leaf; selects layout, does not compute it. Consumed in-process by `apps/theorem-gateway` (SceneOS add-on) and by `apps/theorem-ios`. |
| `scene-os-web` | SceneOS renderer bundle (Lane B): `render_scene(&ScenePackageV2) -> String` embeds a self-contained canvas+d3 renderer (`web/dist/scene-os.bundle.js`, esbuild IIFE from `web/src/`) into `web/scene-host.html` and injects the package JSON via the `scene_payload_json` escape path (`<`,`>`,`&` -> `\uXXXX`; SERP injection pattern). The renderer runs a d3-force settle and a spawn->settle->annotate enter choreography (origin from package provenance; `prefers-reduced-motion` reveals settled directly); model-explanation atoms (`kind:"annotation"`) render as d3-annotation callouts pinned to anchor nodes. Served over HTTP by the gateway's `GET /scene/{id}`. |

## The Two Language Sides + the Bridge

- **Rust** (`rustyredcore_THG/` + `apps/browser*/`): the substrate engine, the browser, RustyWeb, native symbolic engines.
- **Python** (`apps/notebook/`, `apps/orchestrate/`): the mirror of Theseus's inference layer (reference engines, routing kernel, parity + cost gates, evolution callers).
- **Swift** (`apps/theorem-ios/`): the native phone client surface over ScenePackageV2. It currently builds as a Swift Package so Command Line Tools can validate the core before Xcode is available.
- **PyO3 bridge**: `rustyredcore_THG/src/lib.rs` is a `#[pymodule]` exported to Python as **`theseus_native`** (maturin, `pyproject.toml`). The Rust fn is named `rustyredcore_THG` but `#[pyo3(name = "theseus_native")]` overrides the Python-visible module name. Do NOT remove that name override: without it the symbol is `PyInit_rustyredcore_THG` and Python's `import theseus_native` fails silently into the slow Python fallback path.

## GraphStore: three stores, one trait

`rustyred-thg-core` defines the `GraphStore` trait. Three impls, used for different durability needs:

- **`InMemoryGraphStore`** - ephemeral, in-process. Tests + scratch.
- **`RedCoreGraphStore`** - durable, file-backed, in-process (AOF + snapshots). `open(data_dir, RedCoreOptions)`; `RedCoreOptions::default()` is `AofEverysec`, use `AofAlways` when you need fsync-per-commit determinism. **This is the "in-process substrate with no API boundary" the browser persists to.** It implements `GraphStore` (writes delegate to the inherent AOF-backed durable upserts; reads serve from an in-memory mirror that `recover()` rebuilds from the AOF on open). TTL methods keep the trait defaults (durable TTL is a follow-up; delegating TTL writes to the mirror would skip the AOF).
- **`RedisGraphStore`** - connects to a Redis/RustyRed server (an API boundary, out-of-process).

`RedCoreGraphStore` and `RedisGraphStore` also expose inherent `get_node`/`upsert_node` etc. that can **shadow** the trait methods. When you need the trait method on those types, call it via UFCS: `GraphStore::get_node(&store, id)`.

## The substrate-native browser

- **`apps/browser/`** embeds Servo as a Cargo **git dependency** pinned to a known-good rev (NOT a fork). It builds `libservo` via plain `cargo` after `./mach bootstrap` provides system libs (the external-embed path, like Verso). The embedder constructs `ServoBuilder::default().event_loop_waker(...).build()`, a headless `SoftwareRenderingContext`, and a `WebView` via `WebViewBuilder::new(&servo, rendering_context)`.
- **Substrate seam 1 (DOM -> graph state)**: `WebViewDelegate::load_web_resource(&self, webview, WebResourceLoad)` (NOT `ServoDelegate`, which fires only for non-WebView loads). On `notify_load_status_changed(LoadStatus::Complete)`, build a `LoadedPage` and call `theorem_browser_substrate::ingest_loaded_pages` / `durable_browser_session_with_options`, which writes the page into a `GraphStore` (use `RedCoreGraphStore` for persistence).
- **`apps/browser-substrate/`** is the Servo-free seam: `LoadedPage -> rustyred-web (build_v2_fixture_crawl) -> CrawlGraph::apply_to_store`. Keep it Servo-free so it stays cheap to iterate.

## Build & Dev Commands

There is **no root Cargo workspace**. Pick the right workspace/crate:

```bash
# RustyRed/THG core workspace (the engine + bridge)
cd rustyredcore_THG && cargo check --workspace # type-check the workspace; use maturin for PyO3
cd rustyredcore_THG && cargo test -p rustyred-thg-core   # test the core
cd rustyredcore_THG && cargo test --no-run -p rustyred-thg-core  # compile-only coherence check
cd rustyredcore_THG && cargo test -p theorem-harness-core # test the Rust-native harness kernel
cd rustyredcore_THG && cargo test -p theorem-harness-runtime # test GraphStore-backed harness event persistence
cd rustyredcore_THG && cargo test -p theorem-browser-agent # test browser-use policy/perception/action contracts
cd rustyredcore_THG && cargo test -p theorem-copresence # test the headless co-presence peer seam
cd rustyredcore_THG && cargo test -p rustyred-web # test crawl/search plus browser-use web primitives

# The seam crate (Servo-free, fast: no libservo)
cargo test --manifest-path apps/browser-substrate/Cargo.toml

# The gRPC search server (standalone Cargo root, NOT a rustyredcore_THG member)
cd apps/theorem-grpc && cargo build -p theorem-grpc   # binds 0.0.0.0:$PORT (default 50071 local)

# The harness HTTP transport (standalone Cargo root, NOT a rustyredcore_THG member)
cd apps/theorem-harness-server && cargo test
cd apps/theorem-harness-server && PORT=50080 THEOREM_HARNESS_DATA_DIR=harness-data cargo run

# The native iOS client scaffold (CLT-friendly; full simulator/archive needs Xcode)
swift build --package-path apps/theorem-ios
swift run --package-path apps/theorem-ios TheoremIOSSmoke

# The Servo embedder builds in CI only (heavy: ~30 min libservo from cold).
# Trigger: gh workflow run servo-browser.yml --ref main
#   - servo pinned in apps/browser/Cargo.toml (rev) + apps/browser/rust-toolchain.toml (1.95.0)
#   - the workflow reclaims ~20-25GB of preinstalled SDKs + sets CARGO_PROFILE_DEV_DEBUG=0

# PyO3 wheel (theseus_native) via maturin, from rustyredcore_THG
cd rustyredcore_THG && maturin develop        # build + install the wheel into the active venv

# Python parity / cost gates (mirror of Theseus inference)
#   live under apps/notebook/benchmarks/ and apps/notebook/inference_engines/.../tests/
```

## Conventions & Gotchas

- **Mirror discipline**: this is the projection, not the canon. Keep parity receipts + the PyO3 surface reconciled with Theseus. Surface drift, don't bury it.
- **No root workspace**: `-p <crate>` only works inside the relevant workspace (e.g. `cd rustyredcore_THG`); `apps/browser*` are standalone crates reached via `--manifest-path`. Path deps cross from `apps/` into `rustyredcore_THG/crates/`.
- **Byte-parity discipline**: native Rust symbolic engines must byte-match the Python reference receipts. The differential gates live in `apps/notebook/benchmarks/`. A native port is not done until its parity gate is green over the reference set.
- **Servo build is the long pole**: it is CI-only and heavy. Pin the `servo` rev deliberately; bump on evidence. The embedder's `rust-toolchain.toml` must match Servo's (the whole graph builds with one toolchain). `mach` needs its Python deps (`toml`) in the invoking interpreter before its venv activates.
- **Editing a file another agent (Codex) is on**: Codex is frequently active in this repo. Commit only with an explicit pathspec (`git commit -- <paths>`), never a bare `git commit` (the shared index can carry another agent's staged files). Claim a seam before overlapping. The harness `coordinate` endpoint has been returning 503 (down); use git history + commit messages as the coordination channel meanwhile.
- **No emojis. No em/en dashes** (match the user's style across repos: use colons, parens, semicolons).
- **No time/effort estimates** in plans or reports.
- **Dependencies named in a spec are information for you, not gates.** Check the tree, decide, and note your reasoning on the job.

## Status / Current Direction

- **Multimodal planner unify (`relational_query` as the unified cross-modal entry)**: reviewed Codex's `Travis-Gilbert/multimodal-planner-unify` work against `~/Downloads/SPEC-MULTIMODAL-PLANNER-UNIFY.md` and rebuilt the modality layer (CC). The spec thesis is sound and kept: boolean filters intersect through access methods; `Knn`/`TextMatch{Rank}`/`Expand{Rank}` survive as SCORES through a separate fuse phase (RRF default `k=60`, or weighted); the two-trait split (`AccessMethod` filter / `RankingAccessMethod` rank) is the guard. Surfaced + fixed divergence: Codex resolved modality from PRIVATE shadow indexes copied out of relational-row properties, which the spec forbids four times (resolve by id from the live TurboVec/full-text/graph subsystems, copy nothing). Fix = a `ModalityResolver` trait in `rustyred-thg-core/src/access_method.rs`, injected PER-CALL via `execute_query_with_resolver(store, query, &dyn ModalityResolver)` (borrowed; a stored `Arc<dyn _ + 'static>` would fight the borrowed backend), with `execute_query` kept as a `NoModalityResolver` back-compat wrapper so pg-server/GraphQL/tests are untouched. `RankingAccessMethod::rank` now takes candidate NODE-IDS (`Option<&BTreeSet<RowId>>`) + the resolver and returns `RankOutcome { rows, strategy, overfetch_rounds }` (ids, not an ordinal bitmap, which also killed the fragile positional coupling). Shadow state deleted; `ranking.rs` rankers are thin resolver delegators plus the residual-check helpers (`row_coords`/`row_text_matches`). Honest trace: strategy is reported by the resolver, never the cosmetic `filtered_hnsw` (RustyRed's vector index is `turbovec::IdMapIndex`, NOT HNSW): `exact_over_candidates` / `filtered_overfetch` (real overfetch loop + round count) / `index_topk`; `expand` score_source is `hop_distance`. MCP (`rustyred-thg-mcp/src/lib.rs`): `BackendModalityResolver` binds `vector_knn`->`vector_search` (TurboVec, real overfetch-intersect loop), `text_*`->`fulltext_search`, `expand_*`->`neighbors` BFS; wired into `relational_query_payload`; the top-level `fusion` arg is now wired (overrides `query_ir.fusion`). Geo (deliverable 3): `Predicate::GeoWithin` extended with serde-default `lat_property`/`lon_property`/`label`; the adapter routes to live `spatial_bbox_search` when addressed, residual-only exact scan otherwise (traced `geo_index` vs `geo_residual_scan`); the residual prefers the explicit property names. Also fixed two PRE-EXISTING branch defects Codex never built (he ran only core+mcp): pg-server failed to compile (`QueryIr.fusion` + `QueryResult.rows`->`QueryOutputRow`; fixed with `..QueryIr::default()` + mapping rows to flat maps at the pg-wire boundary), and a parallel-test flake (`native_coordination_tools_round_trip_through_mcp` raced the composed-agent test mutating process-global `THEOREM_AGENT_HEADS`; pinned via `ScopedEnv::new(vec![])` + shared `ENV_LOCK`). Validation: core 195 lib + 9 `planner_multimodal_acceptance` (AC2 brute-force ref, AC4 RRF ref, AC3 tightened to a genuine overfetch loop, + an explicit-property geo test) + 8 relational regression; mcp 103 (parallel, +1 end-to-end `relational_query` geo test, the first modality test through the real handler); pg-server 18+10; `cargo check --workspace` clean; changed files clippy-clean. **UNCOMMITTED in the working tree** (the tree is currently on branch `commonplace` from a parallel harness-console session; this work began on `Travis-Gilbert/multimodal-planner-unify`). `rustyred-thg-mcp/src/lib.rs` is co-mingled with Codex's unrelated composed-agent `ProviderHeadInvoker` changes, so committing the planner work cleanly needs hunk-level staging. Named follow-up: a spatial-designation discovery API (or richer `GeoWithin` from the SQL/PGQ layer) so geo uses the live H3/S2 index on the MCP path (residual-only there today). Session learnings in `docs/learnings/2026-06-20-{rustyred-vector-index-is-turbovec-not-hnsw, shared-type-change-blast-radius-build-all-consumers, process-global-env-var-parallel-test-race, acceptance-green-is-not-spec-met-when-acs-skip-the-mechanism}.md`.
- **CommonPlace consumer loop (the consumer projection)**: building per `docs/plans/commonplace-consumer-loop/COMMONPLACE-CONSUMER-LOOP.md` as a self-paced harness loop (take a ready unit, build to acceptance, verify against the running engine, record, next). **F1 (consumer object model) DONE**: new `crates/commonplace` (graph-native over `GraphStore`, content-addressed `File` bodies over a `BlobStore` seam). `Item`/`Collection`/`Tag` write+read-back with metadata/residency/tags/collections; `File` resolves its blob by `sha256:` hash; `Collection` enumerates members via reverse `IN_COLLECTION` traversal. 6 tests green (in-memory `InMemoryGraphStore` + durable `RedCoreGraphStore`+`DiskObjectStore` across a restart), clippy-clean, zero-collision new-crate lane. Divergence (surfaced): graph-native, not "module over `rustyred-thg-catalog`" (Items must be first-class graph citizens for F2/I1; acceptance stays DB-free; catalog stays the tenant/key/billing home for F3). **Update: foundation slice F1+F2+F3 all DONE + GREEN.** F2 (auto-structuring ingest) was co-built by Codex in `crates/commonplace` (`IngestPipeline`/`EmbeddingGraphStore`/`DeterministicEmbedder`: embed -> classify into a collection -> file (folder path) -> link `SIMILAR_TO` -> similarity-search; near-dup entity resolution; 4 tests) and CC-verified. F3 (interoperability API seam) is the new standalone crate `apps/commonplace-api`: a typed consumer GraphQL profile over the F1 model + per-instance API-key auth; 3 acceptance tests (read/write with a key; invalid+missing key rejected; separate instances = separate data) + live HTTP smoke (authed ingest auto-structures over HTTP, unauthed rejected). 11 commonplace + 3 api tests green, both clippy-clean. The two heads co-built F1/F2 in one crate and converged (`store.rs` merged cleanly; the git working tree was the coordination channel). **I1 (ask-over-store) also DONE** in `apps/commonplace-api`: an `ask` query fusing vector + lexical + `SIMILAR_TO`-graph retrieval (reciprocal-rank fusion) with per-item provenance and an `AnswerModel` seam (extractive without a model; generative GL-Fusion is the follow-up); 5 api tests + live HTTP smoke. **I2 (proactive briefing) also DONE**: a `briefing` query surfacing recent (by `updated_at`), newly-connected (by `SIMILAR_TO` degree), and open-thread (open-tagged or unfiled) items. CommonPlace progress: F1+F2+F3+I1+I2 green (6 commonplace + 6 api tests), both clippy-clean. **Codex left the CommonPlace lane** (it added `crates/theorem-copresence`; untouched `crates/commonplace` since F2), so the consumer loop is now CC's lane. **I4 (discovery) + AG3 (MCP access) + X2 (import/export) also DONE** -> the BUILDABLE CONSUMER SLICE of the loop is COMPLETE and the loop HANDED OFF to Travis. Shipped in the consolidated ship branch (green, clippy-clean): F1+F2 in `crates/commonplace` (11 tests); F3+I1+I2+I4+AG3+X2 in `apps/commonplace-api` (10 tests + live HTTP/MCP smokes). `apps/commonplace-api` is fully CC-clean + standalone-committable; `crates/commonplace` includes Codex's co-built F2. The remainder is model/frontend/infra/deploy-gated, not buildable-in-lane now: I3-robust (epistemic claim+NLI = model), CAP1/CAP2 (capture surfaces: clipper/share/voice/email-in = client+infra), AG1/AG2 (harness agents-in-store), V1-V4 (app UI), X1 (Notion/Obsidian connectors = API keys), S1/S2 (sync + e2e), M1-M3 (moat). RECOMMENDED NEXT PHASE (one coherent "make it real" push, Travis-directed): durable RedCore+Disk backing (the HTTP server needs an async-graphql generics-over-store refactor for `RedCoreGraphStore` `Send`; the `commonplace-mcp` bin can go durable trivially) + GL-Fusion/RunPod generative answers + real embeddings (the `AnswerModel` + `Embedder` seams are already in place) + Railway deploy of `commonplace-api`. Lesser follow-ups: native tantivy FTS over the lexical arm, PPR over the graph arm, edge-timestamps for true newly-connected.
- **Stream-based coordination (append-log read by cursor)**: implemented per the stream-coordination spec. Coordination moves from turn-start room polling to append-only event streams read by cursor, passive by default, with an intentional-ping escalation and per-actor selective attention; CRDT stays on the graph (it converges shared STATE, not awareness). The named primitive is `rustyred-thg-core/src/stream.rs` (`StreamUrgency`/`StreamEvent`/`StreamLog`/`StreamRegistry`/`StreamKey`/`StreamDelta`, re-exported from the crate root): the append-only special case of `ordered.rs` -- keyed by a strictly-increasing `u64` ordering token that never moves, so `read_after(cursor, limit)` is an early-stopping `OrdMap` range over `(cursor, ..]` (O(result + log n), nothing in the window missed or duplicated, no merge step). `StreamLog::append` assigns the next token; `ingest` re-inserts an event at its own token (durable rehydration) while keeping `next_token` monotonic; `StreamRegistry` is the local-embedded live-tail transport bundling logs + per-`(actor, stream)` cursors + per-actor subscriptions. The MCP tool surface (`rustyred-thg-mcp/src/lib.rs`) is the durable delta-pull transport and replaces the room-poll surface: `stream_publish` (write), `stream_read` (read; passive-by-default, `advance=true` consumes the window once), `stream_subscribe`/`stream_unsubscribe` (writes). Each event persists as a `CoordinationStreamEvent` graph node (+ `COORDINATION_STREAM_EVENT_OF` edge to a per-`(tenant,topic)` `CoordinationStream` head node that holds the monotonic `next_token` allocator, reserved read-modify-write so concurrent publishers get distinct tokens); cursors are `CoordinationStreamCursor` nodes, subscriptions a per-actor `CoordinationStreamSubscription` node. The durable read rehydrates the matching event nodes into a transient core `StreamLog` and runs the identical `read_after` -- one model, two transports. The **intentional-ping bridge**: an `ask`/`block` publish with a `target_actor` writes the stream event AND calls the existing `write_coordination_message` with `delivery=wake` + the target as a mention, so a warm head drains it at its next Stop-hook mention pass and a cold head hits the existing wake -- the ping reaches the target even if it is not subscribed to the stream (mentions are actor-targeted, not subscription-gated). Scope is `(tenant, topic)` through the shared tenant normalizer (`canonical_stream_key` + `tenant_slug_aliases`; new stream node-id helpers appended to `theorem-harness-runtime/src/coordination.rs`): casing preserved, empty tenant rejected rather than defaulted (the four tools are in `coordination_tool_requires_tenant`). Read-only mode keeps the passive `stream_read` listed but downgrades cursor `advance` to false and hides/refuses the three write tools. Validation: 9 core-lib + 7 mcp acceptance tests green (one per spec criterion: exact ordered delta after a stored cursor with no miss/dup; ping -> target mention drain with wake delivery + passive readers still see the event; concurrent distinct tokens in one total order; publish/read share tenant + empty-tenant rejected; subscription set controls read deltas while a ping still reaches an unsubscribed target; plus read-only no-advance and read-only listing), all 64 prior mcp tests still green, `cargo check --workspace` clean, stream code clippy-clean. Additive only: no existing coordination logic changed. Deferred (named, not cut): a `theorem-harness-runtime` GraphStore-native variant of the four verbs for the harness HTTP server (the MCP crate carries its own `McpGraphBackend` persistence, so the observable tool contract is fully met without it); a true embedded live-tail wake (the `StreamRegistry` is the in-process seam; the durable path is delta-pull); and SSE/push notification of new events (polling/turn-start read meanwhile).
- **GraphQL MCP surface (typed schema over the flat tools)**: implemented per SPEC-GRAPHQL-MCP. `rustyred-thg-mcp/src/graphql/` adds one typed `async-graphql` schema (Memory slice + `graphAlgorithm`) carried by three new transport tools wired into the existing dispatch: `graphql_query` (read), `graphql_mutate` (write, read-only-gated + hidden from tools/list in read-only mode), `graphql_introspect` (SDL). Resolvers WRAP the existing crate-private `*_payload` handlers (no memory logic reimplemented): `memory`->`recall`, `MemoryDoc.related`->`relate`, `MemoryDoc.links`-> the one-hop `MEMORY_RELATES` neighborhood, `memoryDoc`->`get_node`, `memoryArchive`->`self_recall_archive`; mutations `rememberMemory` (outcome present -> `encode`, absent -> `remember`), `reviseMemory`->`self_revise`, `forgetMemory`->`forget`, `createHandoff`->`handoff`; `graphAlgorithm(kind: PAGERANK|PPR|COMMUNITIES|COMPONENTS, seeds, damping, alpha, epsilon, topK, inline)` collapses the eight flat algorithm tools to one field. Integration shape: the backend (`Rc<RefCell<..>>`) is not `Send`/`Sync`/`'static`, so it can't live in async-graphql `Data`; instead the document executes synchronously via `futures_executor::block_on` on the dispatch thread, and resolvers reach the backend through a scoped thread-local invoker installed only for that block (the only `unsafe`: two small lifetime-erasure fns, sound because execution is same-thread and synchronous). Connection-scoped tenancy: tenant is resolved ONCE by `call_tool` and carried on the invoker; NO field accepts a tenant arg (structurally enforced -- the SDL contains no `tenant`), and an empty tenant is rejected rather than defaulted. `graphql_query` runs against an `EmptyMutation` schema so a mutation operation is refused by async-graphql itself. Coexists with the flat tools (additive; all 58 prior mcp tests still green). Validation: 64 mcp tests green (6 new acceptance proving all spec criteria: one-shot memory+related+links; outcome->encode vs plain remember via a surfaced `MemoryDoc.outcome`; graphAlgorithm PAGERANK+PPR; empty-tenant rejection + no-tenant-field SDL; query-refuses-mutation; SDL round-trip + flat tools still answer); graphql module clippy-clean. Code-grounded divergences (surfaced, not buried): a doc's "links array" is not a node property here but its `MEMORY_RELATES` edges, so `links` traverses that edge type while `related` is the general caller-parameterized traversal; `MemoryDoc.outcome` was added to surface recorded encode feedback (also the AC2 distinguishing signal). **North Star A3 (graph domain) added 2026-06-19**: the GraphQL surface now also collapses the flat graph/vector/spatial/full-text/symbolic/bulk tools. New `graph.rs` Query fields `graphNode`, `neighbors`, `graphSchema`, `vectorSearch`, `vectorHybrid`, `fulltextSearch`, `spatialRadius`, `spatialBbox`, `deriveFacts`, `sourceReliability`, `expectedValue`; Mutation fields `designateVector`, `designateSpatial`, `designateFulltext`, `bulkNodes`, `bulkEdges`. To keep the wrap faithful, 11 previously-inlined `call_tool` arms were extracted into `*_payload` fns that BOTH the flat tool and the resolver call (regression-safe); `MemoryInvoker` renamed `GraphqlInvoker`. Typed `SearchHit`/`BulkResult`, `Json` for free-form shapes. 5 new acceptance tests prove each field == its flat tool (76 mcp tests green, workspace clean, A3 code clippy-clean). Divergence: spec's `schema` field built as `graphSchema`; `vectorHybrid`+`spatialBbox` added beyond the spec minimum. **North Star A2 (coordination domain, Codex) + A4 (epistemic domain, CC) added 2026-06-19**: A2 = `graphql/coordination.rs` (room context/intent/record, stream publish+read, work-graph/task-node wrappers, by Codex). A4 = `graphql/epistemic.rs` (CC): Query `epistemicNeighbors`/`epistemicFrontier`/`compileSubgraph`/`shadowPpr` + Mutation `enrichApply`, wrapping the existing `epistemic_*_payload` fns (extracted `epistemic_neighbors_payload` from its inlined arm); 2 acceptance tests prove parity on a populated shadow graph + SDL exposure. A2 and A4 grew the same `mod.rs` (trait/impl/MergedObject) additively in parallel across heads. 81 mcp tests green, new code clippy-clean. A4 divergence: `hippoRetrieve` is server-only (no payload in the mcp crate), intentionally absent not reimplemented. **North Star A5 (code domain, CodeCrawler half) added 2026-06-19 (CC)**: `graphql/code.rs` Query `codeSearch`/`codeContext`/`codeExplore`/`codeExplain`/`codeRecognize`/`listRepos` + Mutation `ingestCodebase`/`reindexCodebase`, all via one `code(operation, args)` invoker method wrapping the existing `code_search_payload` (no arm extraction). 2 tests: codeSearch routes to the same compute_code `operation`+`affordance_id` as the flat tool (the fixture uses the affordance-gateway `invoke_code_search` default; native CodeCrawler is RedCore-backed and proven separately) + SDL exposure. 83 mcp tests green, code.rs clippy-clean. **North Star A5-kg (harness instant-KG half) added 2026-06-19 (CC) -- A5 now COMPLETE**: `graphql/kg.rs` `HarnessKgQuery` { harnessKgStatus/harnessKgSearch/harnessKgPpr/harnessKgImpact/harnessKgRelatedObjects/harnessKgExplainEdge }, all reads via one `instant_kg(operation,args)` invoker method. The 6 inlined instant-KG `call_tool` arms were consolidated into one `instant_kg_payload(.., operation, name)` (same shape as `code_search_payload`); behavior-preserved (existing instant_kg test still green). 2 tests: harnessKgImpact(symbolName:"Ada") seed+results == flat harness_kg_impact + SDL exposure. 85 mcp tests green, clippy-clean. **Area A (GraphQL MCP surface) is now feature-complete through A5**: 6 domains (memory/graph/coordination/epistemic/code/kg) collapsing ~70+ flat tools. **North Star A6 (remaining clusters) added 2026-06-19 (CC)**: `graphql/clusters.rs` `ClustersQuery` { harnessRun/skillList/skillGet/ensembleSelect/jobList } + `ClustersMutation` { skillPublish/skillApply/ensembleRegister/jobSubmit/jobNote/jobArchive }, via 4 operation-param invoker methods (harness_run/skill/ensemble/job) wrapping existing payloads (no extraction). 3 tests: skillList+harnessRun parity, ensembleRegister->ensembleSelect round-trip, jobList routing (RedCore-backed: fixture gates job_* like code), SDL exposure. 88 mcp tests green, clippy-clean. web/browse/fractal are SERVER-ONLY (punt to async server; not wrapped here, like hippoRetrieve). **Area A is feature-complete A0-A6** (8 domains: memory/graph/coordination/epistemic/code/kg/clusters); only A7 cutover remains. **North Star A7 (cutover) added 2026-06-19 (CC) -- AREA A IS COMPLETE**: default-off `McpServerConfig.graphql_default_surface` + `const GRAPHQL_COVERED_FLAT_TOOLS` (~50 confirmed names) + a `tool_definitions` `retain` filter that hides the covered flat tools from tools/list when the flag is on (graphql_* transport + uncovered tools always stay). 2 tests: default-off unchanged + covered-list-has-no-dead-names + flag-on hides covered/keeps graphql+uncovered; and a full task (rememberMemory -> memory -> graphAlgorithm) through graphql_mutate/query ALONE. 90 mcp tests green, clippy-clean; additive (default off = no behavior change). **Area A = A0-A7 COMPLETE: the GraphQL MCP surface (8 domains: memory/graph/coordination/epistemic/code/kg/clusters) collapses ~50 flat tools into one typed schema with an opt-in GraphQL-default mode.** Honest divergences: web/browse/fractal/hippoRetrieve server-only (not wrappable in-crate); jobs/code RedCore-backed (routing-proven on fixture); coordination/stream/multihead partially covered by A2 (kept visible in cutover, future audit can extend the covered-const). Deferred (named, not cut): a persisted `.graphql` SDL snapshot for client codegen; the loop now moves to Areas B/C/D/E/F (verify built-vs-unbuilt before rebuilding). **North Star E0 (embedded-mode) planning spec written 2026-06-19 (CC)**: `docs/plans/rustyred-multimodel/E0-embedded-mode.md` -- a `[spec to write]` unit (the map authorizes these as units). Observed C2 SQL/PGQ is blocked on B0 (planner registers only Ordered+TimeSeries access methods, no graph-traversal one) and the heavy core units (B0/B4/D1) are Codex-collision-prone, so picked the zero-collision E0 planning unit (E0 = the North Star headline "done": link crate / drop single binary / run local / no server). Grounded in RedCoreGraphStore::open->recover (durable in-process spine, restart rehydration already works at store layer) + the in-process Area A GraphQL surface; defines E0.1-E0.5 with acceptance. **North Star E0.1a (embedded seam) added 2026-06-19 (CC)**: `rustyred-thg-mcp` now exports `pub struct SharedStore<S>(Rc<RefCell<S>>)` -- a cheap in-process handle that impls BOTH `McpGraphProvider` (backend_for_tenant clones the Rc) and `McpGraphBackend` (forwards all 42 trait methods to the inner S, so a non-Clone durable `RedCoreGraphStore`'s 27 overrides are preserved). `SharedStore::new(store)` passes straight to `handle_mcp_request`, so the full typed GraphQL surface runs over one owned durable store in-process, no socket/async runtime. Test `embedded_shared_store_runs_graphql_in_process_over_redcore` (bulkNodes mutate + neighbors query over SharedStore<RedCoreGraphStore::memory()>). 91 mcp tests green, clippy-clean. Next loop unit: E0.1b -- the thin `apps/rustyred-embedded` standalone crate (Engine::open(dir) = RedCoreGraphStore::open -> SharedStore -> handle_mcp_request) + E0.2 restart-rehydration. Then Areas B-F (verify built-vs-unbuilt before rebuilding).
- **EpistemicRAG (shadow epistemic graph)**: implemented per `SPEC-EPISTEMICRAG.md` + `SPEC-EPISTEMICRAG-INSTANT.md`. A parallel shadow layer over the per-tenant content graph: each content node can gain an `EpistemicShadow` node (a first-class node label in `rustyred-thg-core`, so existing PPR/HNSW/community machinery run on it directly) carrying neurosymbolic epistemic standing, joined by `HasEpistemicShadow` and connected to other shadows by `Undercuts`/`Supports`/`SameEClass`. The content graph is never mutated; only shadow nodes and shadow edges are written. Co-built with Codex. **Codex (core/code/memory)**: `rustyred-thg-core/src/epistemic.rs` (the `EpistemicShadow` schema with per-field provenance, `structural_epistemic_pass` = Tier-1 topology [orphan/bridge/unsupported-leaf/support+attack in-degree via the existing CSR `articulation_points_and_bridges`/`connected_components`/`strongly_connected_components`] + grounded-extension status + idempotent shadow writeback keyed by `(content_id, engine_version)`; `epistemic_shadow_ppr` over the shadow layer; the `EpistemicEnricher` trait + `run_epistemic_cron_pass` with the density-floor gate and graceful gRPC-drop no-op; `UserSubgraph`/`EpistemicAnnotations` contract types); `rustyred-thg-code/src/code_epistemic_hook.rs` (the INSTANT synchronous pass at ingestion: claim extraction, `vector_search` HNSW-bounded candidate pairs scoped by repo/generation, cheap doc-claim drift, registered in `code_hooks.rs`, surfaced through `context_pack.rs`'s `epistemic_readout`); `rustyred-thg-memory` recall `include_epistemic` (joins `read_epistemic_shadow`, attaches the shadow readout, default unchanged). **CC (open lanes, zero source overlap)**: the gRPC enrich contract `apps/theorem-grpc/proto/theseus_epistemic/v1/epistemic.proto` (Theorem->Theseus, mirroring the Rust types); the Modal population cron `apps/orchestrate/runtime/epistemic_cron.py` (delta-threshold + nightly-full schedules, Python gRPC client to Theseus, MCP writeback, load-bearing graceful-degradation no-op; 4 cron-policy tests green); and the cross-spec acceptance suite `crates/rustyred-thg-core/tests/epistemic_acceptance.rs` (11 tests proving both specs end-to-end: Tier-1-no-model, planted-contradiction->undercut->grounded-marks-loser-out, HNSW-bounded-not-quadratic, source_kind=structural, structural idempotent; cron adds learned preserving structural, predicted-edges quarantined+not-injected-as-nodes, gRPC-drop clean no-op, cron idempotent by engine_version, density-floor gate, shadow-layer PPR). Validation: 162 core lib + 11 acceptance + 1 memory + code-crate epistemic green; 4 Python cron tests green. Code-grounded divergences (surfaced, not buried): the shadow node id keys on `(content_id, engine_version)` with a shared default version so structural+learned land on one node and the edge-based reverse lookup stays robust (a literal reading of the spec idempotency key, not a per-node collapse); grounded_extension_status uses the simplified `attacked->out, cycle->undecided` rule (sufficient for the spec acceptance; full least-fixed-point AF is a refinement); code drift is a readout finding not a shadow edge (no target node exists for a missing entity); the gRPC client is Python in the cron (not a Rust tonic client), with the Rust `EpistemicEnricher` trait as the in-process seam. Deferred (named, not cut): the remote-driving MCP/HTTP writeback endpoint that wraps `run_epistemic_cron_pass` (a `McpGraphBackend::apply_epistemic_annotations` calling it with a static enricher; reads already covered by recall `include_epistemic`), the live Theseus enrich gRPC server, and the Modal deploy.
- **EpistemicRAG dedup (Strand A phase 3 / cut 9)**: implemented per the consolidated backlog index (`~/Downloads/theorem-harness-backlog-index-v2-consolidated.md`), the next unbuilt step in the strand's lead. `rustyred-thg-core/src/epistemic.rs` gains `epistemic_egraph_dedup` (+ `EpistemicCongruence`/`EpistemicDedupConfig`/`EpistemicDedupReport`/`EpistemicEquivalenceClass`, `read_same_eclass`, `same_eclass_edge_id`, `EGRAPH_EPISTEMIC_ENGINE`, and an additive `same_eclass` field on `EpistemicShadowReadout`): collapses provably-equivalent epistemic shadow STATES many-to-one onto a single `SameEClass` representative using an `egg` e-graph (new `rustyred-thg-core` dep `egg = "0.10"`, already resolved via the PyO3 root crate). Each shadow becomes the term `(shadow <grounded-status> <claim-term>)`; negation is encoded structurally `(not (not p))` and the rule `(not (not ?x)) => ?x` reduces parity, so e.g. "enabled" and "not not enabled" collapse by proof where the prior string-tuple `dedupe_relations` could not (the congruence predicate = the spec's named open item; default `ClaimAndStanding` keeps `in`/`out` states apart, `ClaimOnly` merges across standing). Grouping is driven by egg `find()`, so richer rules re-cluster with no grouping-code change. Maximally non-breaking: writes ONLY `SameEClass` edges (never mutates content nodes or shadows); `epistemic_shadow_ppr` already traversed `SameEClass` so the edges light up clustering for free, and recall surfaces membership via `read_epistemic_shadow().same_eclass` (read off the edge). Validation: 166 core lib + 17 acceptance green (9 new cut-9 tests: double-negation collapse, single-neg-stays-distinct, many-to-one+idempotent, content-graph-untouched, SameEClass-feeds-PPR, recall-readout surfacing, ClaimAndStanding-vs-ClaimOnly, plus a >10k-e-graph-node scale regression); new code clippy-clean. A 4-lens adversarial-review workflow (egg-correctness / non-breaking / congruence-soundness / spec-completeness) proved the non-breaking + determinism + no-self-edge + stable-class_id invariants and caught one major bug now fixed+regression-tested: `Runner::default()`'s 10_000 `node_limit` silently disabled ALL merging past ~3.3k claims (egg short-circuits rewrite-apply on `NodeLimit`, so the rule fired on zero terms); `egraph_class_roots` now lifts node/iter limits (safe -- the rule only shrinks the graph, always saturates). Code-grounded scope notes (surfaced, not buried): negation parity is best-effort (whole-claim lexicon shared with `without_negation`, not scoped logic; a mis-parity only fails-to-merge or mislabels the cosmetic `canonical_form`, never corrupts the content graph); full-store mode bounds at 100_000 shadows (module convention; drive larger tenants with an explicit `content_node_ids` batch). Deferred (named, not cut): the WRITE trigger (a cron/MCP/server caller of `epistemic_egraph_dedup`; the read path is already wired via recall); egglog as the backend for the byte-parity Datalog `derive_datalog_receipt` (an egglog rewrite would break its Python-reference parity gate -- this e-graph is structured as the reusable substrate for that upgrade).
- **Storage spine (larger-than-RAM memory tiering)**: implemented per `~/Downloads/theorem-harness-handoff-6-storage-spine.md` (handoff 6). The operating store stays RAM-first; the cold tail spills to disk; eviction stops being a scan. Core (`rustyred-thg-core`): `object_store.rs` (the disk-backed content-addressed cold object store + in-memory variant; the cold tail's durable source-of-truth home, NOT Valkey), `cold_index.rs` (the synchronous `ColdIndex` catalog trait: id -> {tier, object-hash, scope} + in-memory/disk impls; makes rehydration a keyed lookup, never a repository scan), `ordered.rs::EvictionFrontier` + `OrderedIndex::range_to` (per-tenant-scope persistent ordered index over `last_accessed_ms`; coldest-below-cutoff is an early-stopping range, O(k log n)), and version-neutral `evict_node`/`readmit_node`/`evict_edge`/`readmit_edge` on `GraphStore` (residency changes that never touch `stats().version`, so the `ppr_cache.rs` priors of the live nodes stay valid -- the open item). `versioned_graph.rs` exposes `node_to_content_object`/`node_from_content_object` (+ edge) for the single-object commit-on-evict, and warm-scope rehydration reuses `checkout_graph_version`. Memory (`rustyred-thg-memory`): `ColdTier`, `evict_decayed` (flag -> commit -> record -> evict -> forget), `rehydrate`, `recall_with_cold_tier`, `seed_frontier`, `park_scope`/`unpark_scope`. Catalog (`rustyred-thg-catalog`, new sqlx/Postgres crate): tenants/projects/billing/auth + `cold_index`/`cold_scope` rows; `PostgresCatalog` (async) + `PostgresColdIndex` (sync `ColdIndex` bridge). All 5 acceptance criteria proven in `crates/rustyred-thg-memory/tests/storage_spine_acceptance.rs` (evict-yet-rehydrate; O(k log n) frontier-ops counter, candidates_examined == k not n; durable across an operating-store restart on the disk store; warm-scope round-trip; eviction leaves the store version unchanged). 174 core + 25 memory + 3 catalog tests green; new code clippy-clean. Code-grounded divergence (surfaced, not buried): the cold tail is backed by a dedicated `ColdObjectStore` (disk + Postgres seam) rather than retrofitting a disk backing onto `GraphVersionRepository`'s in-memory `BTreeMap`s -- same outcome (cold tail on disk, reused content-object + `checkout_graph_version` readers, keyed rehydration), less blast radius on the versioned-graph MCP path. Deferred (named, not cut): MCP/server wiring of `evict_decayed`/`rehydrate`/`park_scope` (the spec's Files/acceptance are core+memory+catalog; MCP exposure is a natural follow-up); RedCore `evict_node` override (the durable operating store keeps the safe `GraphStore` defaults; acceptance uses `InMemoryGraphStore`); write-time frontier touch (recall is the zadd site today, `seed_frontier` bootstraps pre-existing nodes).
- **Engine view tiers (DOCUMENT-TIER + PG-WIRE, on the relational core)**: three engine specs, co-built live with Codex. (1) **RELATIONAL-CORE** (`SPEC-RUSTYRED-RELATIONAL-CORE.md`): Codex-built (`access_method.rs`/`planner.rs`/`relational.rs`/`cold_fragments.rs`/`working_log.rs`), CC-verified 10/10 acceptance (see `tests/relational_core_acceptance.rs`); uncommitted; do not re-implement. (2) **DOCUMENT-TIER** (`SPEC-RUSTYRED-DOCUMENT-TIER.md`): Codex built `doc_tree.rs` (CoW `OrdMap` path-keyed B-tree, NUL-separator prefix isolation, `OrdMap::range` prefix scan, inline/overflow `DocEntry`, `previous_hashes` versioning) + `object_store.rs` zstd document store (hash over uncompressed). CC added `tests/document_tier_acceptance.rs` (7 spec criteria; AC4 uses the known-answer sha256("abc") to discriminate hash-over-raw-vs-compressed; AC7 inserts the promoted dock into `InMemoryGraphStore` and re-finds it by `NodeQuery::label`). Flagged-to-Codex gap still open: `DocTree` does NOT `impl ColdIndex` and is wired nowhere, so the spec's headline "retire the Postgres cold index" (AC2) is not fully satisfied by the document namespace alone. (3) **PG-WIRE** (`SPEC-RUSTYRED-PG-WIRE.md`): CC built `rustyred-thg-pg-server/src/relational_server.rs` (`serve_relational`) on Codex's crate scaffold: a blocking std::net Postgres-wire server that parses SQL (sqlparser 0.62 AST) -> planner `QueryIr` -> `execute_query` over a `RelationalStore` of native views. Simple + extended protocol with `ParameterDescription` (so `tokio-postgres` prepare works), Bind result-format honoring (text + binary `DataRow`), type-inferred OIDs (text/int8/float8/bool), parameterized `$N` (Parse OIDs -> Bind value decode -> placeholder substitution; undeclared params default to TEXT to avoid `pg_catalog.pg_type` LEFT-JOIN introspection a real driver fires), `time_range()` modality hitting the time-series index (knn/geo/text REJECTED pending core access-method wiring -- refuse, not return unfiltered), ORDER BY/GROUP BY+count/sum/min/max/avg(DISTINCT)/LIMIT in the frontend, EXPLAIN. `main.rs` defaults to `serve_relational` (`RUSTYRED_THG_PG_MODE=executor` for Codex's ThgExecutor surface). Boundary: native-views-only; auth/billing are the real-Postgres (Neon) escape hatch, NOT wire views (`NativeCatalog` kept but dormant). Acceptance `tests/pg_wire_acceptance.rs` drives a live `tokio-postgres` client over a socket (AC1-AC7); 17 unit + 10 integration green, CC code clippy-clean. A 4-lens adversarial review (pgwire-completeness/doctier-rigor/boundary-soundness/bughunt) caught + CC fixed real silent-wrong-data bugs: duplicate output-column collapse (now positional rows), `count(DISTINCT)` dropped, modality stubs returning unfiltered rows. Deferred (named, not cut): param-query result columns type as TEXT (statement Describe has no values/schema-types; literal SQL keeps full typing); knn/geo/text modality + `timestamptz`/`bytea` OIDs need core (`AccessMethodRegistry`/`ScalarValue`) work = Codex lane; `pg_catalog` introspection beyond OID-avoidance not served; codec is duplicated across `lib.rs`/`relational_server.rs` (consolidate once Codex's pg-server lane settles). Tenant-partition coordination bug (Claude.ai found, CC verified/enriched) is logged as room tension `record_9fa3a5cf9dcada25` in Codex's tenant/project-scope lane.
- **Graph-level hook primitive (reactive substrate)**: implemented per `~/Downloads/rustyred-hook-primitive-spec.md`. `rustyred-thg-core/src/hooks.rs` wires the `Hook` capability that `PluginCapabilityKind::Hook` only declared: `MutationEvent`/`MutationMatcher`/`HookRegistration`/`HookContext`/`HookOutcome` plus a `HookDispatcher` (a tokio-free `std::thread` worker over a bounded, in-place-coalescing buffer; loop-guard by per-event `depth` < `max_depth`; fail-open via `catch_unwind` so a handler panic never poisons the store mutex; `quiesce` barrier). `graph_store.rs` emits post-commit/post-publish events from `commit_batch` (NodeUpserted/EdgeUpserted, with a `changed_props` diff vs the live store) and `purge_expired_nodes` (NodeDeleted), gated on an attached `HookEmitter` (additive `hook_emitter`/`hook_emit_depth`/`hook_tenant` fields; no-op by default). `RustyRedPlugin::hooks()` + `PluginRegistry::hooks()` collect registrations. Three handlers ship as plugins: `rustyred-thg-code` (`code_hooks.rs` IncrementalCentrality = localized PPR warm onto `CodeSymbol.centrality`; `code_embed_hook.rs` IncrementalEmbed = deterministic offline embedding onto a `(CodeSymbol, embedding)` vector designation; wired into `CodeIndexRuntime` behind `THEOREM_CODE_HOOKS` (default off) + `start_code_kg_dispatcher`), and `rustyred-web` (`crawl_hooks.rs` FetchCompletion = source-classify + entity extraction from the `ContentSnapshot` text + event-driven `PprPrioritizer.recompute`; LinkDiscovery = PPR-seeded initial priority; shipped as `RustyWebHooksPlugin`). 337 tests green across core/code/web (acceptance 1,3,4,5,6 proven by tests). Code-grounded divergences: centrality coalesces by a constant funnel key then re-groups by the `repo_id` property (node ids are content hashes, repo_id not parseable from the event); real edge-type names `CALLS_SYMBOL`/`DEPENDS_ON_SYMBOL`/`DECLARES_SYMBOL`; embed triggers on `signature`/`snippet`/`search_text`; `EdgeDeleted` is a defined-but-unemitted variant (RedCore has no standalone edge-delete commit). Deferred (named, not cut): acceptance #2's context_pack latency drop is a deployed large-repo measurement (mechanism built + env-flagged, not yet benchmarked); live wiring of crawl hooks into the async `CrawlRunner`.
- **HippoRAG 2 hubs + rerank-before-gate search**: implemented from `SPEC-HIPPORAG2-HUBS-1.0` and `SPEC-SEARCH-RERANK-GATE-1.0`. `rustyred-hipporag` indexes `Page` passages into `Phrase` nodes, writes `CONTAINS`/`RELATES`/`SYNONYM` edges, builds graph-resident `Hub` nodes with `SUMMARIZES`/`HUB_PARENT`, exposes query-specific PPR retrieval with hub priors, and returns raw membrane `Candidate`s. `HippoTextEmbedder` adds the real-vector path: `index_passage_with_embedder`, `build_summary_tree_with_embedder`, and `retrieve_with_embedder` write/use `semantic_vec` plus model metadata on graph nodes while keeping candidates lean. `rustyred-web` bridges its Qwen embedding client into that trait, and `rustyred-thg-server` uses configured Qwen envs for `hippo_retrieve` / `web_search_graph` query vectors, falling back to deterministic hash vectors when no endpoint is configured. Focused validation green: `cargo test -p rustyred-membrane -p rustyred-hipporag -p rustyred-web -p rustyred-thg-fractal -p rustyred-thg-server`. Live model/service benchmarking remains a separate deployment measurement.
- **Rust theorem symbolic engines**: implemented through RT-5.2a (native evolution parity). Remaining RT-5 ports (e-graph generalization, the cold engines) stay demand-driven / profile-gated. Plan: `docs/plans/rust-theorem-symbolic-engines/`.
- **Connector-as-substrate affordance learning**: `rustyredcore_THG/crates/rustyred-thg-affordances` is a new workspace member that turns MCP connector tools into learnable `Affordance` graph nodes (registry, invocation receipts, proactive scoped selection via PPR plus cosine with a cold-start forwarding fallback, training export plus pairformer writeback). 15 unit tests green, including durability across a `RedCoreGraphStore` reopen. Plan/spec: `docs/plans/mcp-learning-layer/`. Note the surface overlap with the model-training substrate Codex committed in `rustyred-thg-adapters` (`training_substrate.rs`, commit `8d696e0`): different subject (tool selection vs model-training data) but duplicated graph-vocabulary constants (`EVALUATED_BY`, `PROMOTED_TO_ACTIVE`, `TRAINED_ON`, `MODEL_ARTIFACT_LABEL`, `EVALUATION_RECEIPT_LABEL`) and structurally parallel fitness/selection machinery. Open decision (Travis): keep as a sibling crate vs fold into adapters, and hoist the shared edge/label vocabulary into `theorem-harness-core`.
- **Live MCP connector transport (the affordance layer's transport half)**: `rustyredcore_THG/crates/rustyred-thg-connectors` is the outbound mirror of the inbound `rustyred-thg-mcp` adapter. It connects to an external MCP server over stdio, performs the `initialize` -> `notifications/initialized` -> `tools/list` handshake, and feeds each tool through `rustyred-thg-affordances::register_connector` so it becomes a learnable `Affordance` node. Pure JSON-RPC protocol layer (request builders, response parsers, `ToolDescriptor` -> `ConnectorManifest` mapping) + a thin `BufRead`/`Write` stdio transport with id correlation (tokio-free; framing tested over in-memory buffers) + the `connect_and_register` bridge. 14 tests green; the affordances crate is untouched (its 15 tests still green). Closes the affordances plan's named "Live MCP tools/list ingestion" seam. Deferred next slices (named, not cut): the `tools/call` invoke bridge + `record_invocation` (needs persisting the `ConnectionTarget`); a driving surface (admin MCP tool on `rustyred-thg-mcp` or an HTTP route); and iOS consumption (the Connectors surface stays an honest empty state until the backend is real). Plan: `docs/plans/mcp-learning-layer/connector-transport-plan.md`.
- **Rust-native harness port**: Phase 1 kernel plus replay/fork and toolgraph selection are implemented in `rustyredcore_THG/crates/theorem-harness-core`. The crate ports the pure Theseus harness kernel (`contracts.py`, `state_hash.py`, `state_machine.py`, `replay.py`, `toolgraph.py`) into Rust: run state, transition guards, content-addressed hashes, cache/oracle/CUA side events, CMH handoff transitions, replay/fork helpers, and permission-aware toolkit compilation. `theorem-harness-runtime` persists transition receipts into a `GraphStore` as run/event nodes and append-chain edges, and now carries native coordination room/intent/presence/message plus event/decision/tension/reflection record primitives. `rustyred-thg-mcp` exposes the native coordination tools, durable write/readback, bundled turn-start context packets, structured contribution capture, optional policy receipts for durable writes, and `harness_append_transition` / `harness_run` over the runtime event log. `apps/theorem-harness-server` exposes run list/detail plus coordination read endpoints over HTTP. For one populated store across MCP and HTTP, point the harness HTTP server at the same RedCore tenant directory the MCP server writes. Coordination is tracked in `docs/plans/harness-rust-port/` while direct THG mirror writes still report tenant resolution degradation.
- **RustyWeb**: active. `rustyred-web` holds the V0 fixture crawler kernel + V2 hardening (budget, URL guard, scope, `CrawlReceipt`) plus the first browser-use web route: `web_consume_to_graph` fetches a page through the fetch cascade, observes it as `PageState`, extracts links/text, and can ingest it into the quarantined `open_web_unverified` graph layer. The graph-yielding search path now has provider acquisition, warm-substrate recall, HippoRAG candidate union, rerank-before-gate admission, deferred handle recovery, and MCP exposure through `web_search_graph`; the older **V1 search layer** plan (`docs/plans/rustyweb-v1-design/`) is still the product/AnswerDraft/license-tiering design reference rather than a fully shipped answer surface. "V0->V2" refers to the crawler only.
- **Substrate-native browser / browser-use agent**: the external Servo embedder builds green in CI (`apps/browser`, libservo as a pinned Cargo git-dep). The page->substrate seam (`apps/browser-substrate`) ingests pages into a `GraphStore`, and `RedCoreGraphStore` now implements `GraphStore`, so browser-ingested pages persist durably to the in-process substrate. `theorem-browser-agent` now ports the browser-use policy/perception/action contracts, while `rustyred-thg-mcp` and `rustyred-thg-server` expose `web_consume`, `browse_with_me`, and `browse_for_me` through MCP/HTTP. Current low-level arbitrary-page execution uses RustyWeb's fetch cascade; Servo remains the heavy render/embedder path until true completed-document extraction is available from the embedder hook. **Cut 7 (live browser action loop through the harness)** per `~/Downloads/theorem-harness-handoff-7-servo-action-loop.md`: `rustyred-thg-server/src/browser_pool.rs` adds an optional live-browser pool behind a `LiveBrowserPool`/`LiveBrowserSession` seam, with `RemoteBrowserPool` as the first transport (an HTTP sidecar gated on `THEOREM_LIVE_BROWSER_ENDPOINT`, pool-size-bounded by a tokio `Semaphore` via `THEOREM_LIVE_BROWSER_POOL_SIZE` (default 4); each checkout holds an owned permit that frees the slot on drop). `AppState` holds the pool plus a `live_browser_sessions` registry; `execute_live_browser_use` routes `browse_with_me`/`browse_for_me` `Action`s through `rustyred_web::run_action` (the driving model is a caller-supplied parameter via the `next_action`/`action` MCP arg), with `browse_with_me` holding a session across calls for per-action preview/veto + human-demonstration capture, while `web_consume` stays cascade-backed. Absent a configured pool, all three behave exactly as before (additive, dark by default). Transport decision (the spec's open item): the offloaded sidecar keeps `rustyred-thg-server` Servo-free; in-process / RunPod variants swap behind the same trait without touching D1/D2. All 6 observable acceptance criteria proven by `rustyred-thg-server` tests (browse_for_me real `run_action` actuation + DOM change in the closing perception + ledger records the actuation; browse_with_me preview-then-confirm; policy/robots-gate derivation; web_consume read-only/no-write + cascade-only; no-pool cascade parity; pool-size bound). Co-built with Codex: Codex landed the server pool + routing (acceptance #2/#3/#5); CC added the pool-size bound + acceptance tests #1/#4/#6 after converging away a redundant parallel `pilot_core` pool seam onto Codex's single abstraction. The rendered-DOM actuation itself is the sidecar/embedder's job; the Rust server drives it through the `run_action` contract.
- **Theorem iOS v1**: first scaffold landed. `scene-os-core` exposes the mobile projection catalog plus layout-only reprojection, availability, and center-node helpers for `force_graph`, `radial_rings`, `tree_layout`, and `fractal_expansion`. `apps/theorem-ios` compiles locally with SwiftPM and includes the SwiftUI shell, Dynamic Island, projection picker, and smoke executable. Next: Xcode project, UniFFI/staticlib `.xcframework`, hosted streaming API wiring, and Grape-backed force renderer.
- **Dispatch v2**: implemented per `docs/plans/dispatch-queue/dispatch-v2.md` (supersedes the strict claim/status lifecycle in `HANDOFF.md`). A typed `Job` node lives in the GraphStore beside `RunState`: pure domain in `theorem-harness-core/job.rs` (Job + Priority/TargetHead, optional `spec_ref` or `spec_inline`, derived state from `started_at`/`archived_at`, append-only receipts), persistence + the four board verbs in `theorem-harness-runtime/job_queue.rs` (submit/list/note/archive over any `GraphStore`, with read-time migration for legacy statuses). The verbs are exposed as MCP tools in `rustyred-thg-mcp` and wired on the product server's `ProductMcpBackend` (`rustyred-thg-server`) through the `RuntimeTenantMirror`. Execution is `theorem-receiver`: a local spawn of the already-authenticated `claude`/`codex` CLI after a set-once start note, zero GitHub Actions/runners/PATs/OAuth tokens. `spawn_session` + `.github/workflows/theorem-handoff.yml` stay as an untouched legacy lane. Deferred (named): SSE jobs-channel wake (polling meanwhile), parallel `capacity > 1` launch.
- **theorem-grpc**: Theorem's first gRPC server is live as its own standalone crate + Railway service. Serves `theseus_search.v1.SearchService` in pure Rust over the RustyRed substrate (no PyO3, no Django). URL-swap target for civic-atlas-server (`THEOREM_SEARCH_URL`); `Search` is real graph rank. Build from `apps/theorem-grpc/` (standalone root).
- **theorem-gateway**: implemented per `~/Downloads/SPEC-theorem-graphql-gateway-1.0.md` (deliverables 1-9). Browser-facing GraphQL gateway at `apps/theorem-gateway` (standalone Cargo root, bare `[workspace]`): Axum + async-graphql 7 + tonic 0.12/prost 0.13 (pinned to match theorem-grpc + the civic client). `build.rs` compiles the vendored protos CLIENT-side (`build_client(true).build_server(false)`); the core gateway has no THG path-deps (the SceneOS add-on below adds two). Resolvers (`src/schema/`): 7 read queries (`search`/`gapWalk`/`provenance` over `SearchService`; `searchCode`/`exploreCode`/`codeContext`/`explainCode` over `CodeCrawlerService`), 2 build mutations (`ingestCodebase`/`reindexCodebase`, allowlist-guarded), and the showpiece `askAgent` (assembles code-KG via searchCode->exploreCode->codeContext or instant-KG via gapWalk, POSTs GL-Fusion, returns the answer next to `contextNodes`). `GlFusionClient` (reqwest) isolates the one open item (the GL-Fusion serving contract); unset `GLFUSION_URL` returns the assembled context with an honest "not configured" answer. Browser boundary: CORS (`CORS_ALLOW_ORIGINS`), public ingest allowlist (`PUBLIC_INGEST_ALLOWLIST`, fail-closed, refuses before any dial), per-IP token-bucket rate limiting (in-memory default, atomic Valkey Lua when `VALKEY_URL` set) on the 3 costly ops, optional recomputable response cache. `--export-schema` emits the committed `schema.graphql`; GraphiQL at `GET /graphql`, `/healthz` -> ok. Own `Dockerfile` (`rust:1.95-bookworm`, context = repo root) + `railway.toml` (own service, `healthcheckPath=/healthz`). 27 unit tests green; `cargo check` + clippy clean; runtime smoke proven (healthz, GraphiQL, introspection, allowlist refusal instant vs allowlisted-passes-then-dials, honest-empty-on-dead-backend, `/scene/{id}` empty-state). Live AC2/AC3/AC4 (ingest+search round-trip, explore edges, askAgent answer) need theorem-grpc + GL-Fusion deployed. **DEPLOYED 2026-06-18** to Railway as a GitHub-connected service in the Theorem project: `https://theorem-gateway-production.up.railway.app` (live /healthz + GraphiQL + introspection + `search` honest-empty over live theorem-grpc + `sceneForInput` + `GET /scene/{id}` all proven). Deploy gotcha: `railway up` (local upload) silently drops the gitignore-negated `include_str!` scene bundle (`!/dist/scene-os.bundle.js`), so the GitHub-connected build (which clones the git tree) is the working path (see `docs/learnings/2026-06-18-railway-up-drops-gitignore-negated-includes...`); monorepo Dockerfile needs `RAILWAY_DOCKERFILE_PATH`. Open follow-ups: `CORS_ALLOW_ORIGINS` unset = permissive (tighten to the website origin); `GLFUSION_URL` unset = honest "not configured" answers; the website tile (in the parent Index-API repo, `src/app/(main)/experiments/scene/`) needs `NEXT_PUBLIC_THEOREM_GATEWAY_URL` set to the live gateway. Code-grounded divergences in the crate README: client generated from vendored protos (not the cross-repo `theseus-client` path-dep); ingest is async + polled to completion; `IngestReceipt.edgeCount` honestly 0; tenant is gateway-config (`GATEWAY_TENANT_ID`, default `gateway-public`). **SceneOS add-on** (per the spec's add-on section): the gateway now path-deps into `scene-os-core` + `scene-os-web` (Dockerfile copies the whole crates dir). Deliverable A (`src/schema/scene.rs`): `sceneForInput(input, scope, origin)` reuses askAgent's context assembly, computes degree centrality + WCC communities + backbone, anchors the GL-Fusion answer as an annotation atom, compiles a `force_graph` `ScenePackageV2` via `scene-os-core`, and stores it in a bounded in-memory content-hash-keyed `SceneStore`, returning `SceneRef { sceneId, url }`. Deliverable B (`src/scene_serve.rs`): `GET /scene/{id}` renders via `scene-os-web::render_scene` (XSS-escaped injection). Deliverable C (`scene-os-web/web/src` + rebuilt `dist`): extended the d3 renderer with origin-spawn -> force-settle -> annotate phases (rAF tween, eased fade), `prefers-reduced-motion` reveals settled directly, annotation atoms render as scoped d3-annotation callouts pinned to anchors. typecheck + esbuild build clean; the gateway re-embeds the rebuilt bundle (verified the served page carries the new markers). Deliverable D (the Next.js `/experiments` omni-bar tile) is a separate-repo consumer (this repo has no Next.js frontend; `apps/desktop` is Tauri) -- the gateway provides exactly what it embeds. Live pixel rendering of the animation is unverified (no Chrome on the build host). Open item still the GL-Fusion serving contract.
- **Obsidian sync**: Phase 1 (pull) + Phase 2 (write-back) implemented. Harness side adds one read endpoint (`GET /v1/tenants/:tenant/memory/docs`, since/include_inactive, server-computed `content_hash`) in `rustyred-thg-server` plus the `upsert_note` MCP tool in `rustyred-thg-mcp` (create-or-update by stable doc_id + `MEMORY_RELATES` link reconciliation: resolved links become edges, removed links tombstoned, forward refs recorded unresolved and reconciled on target creation). Core lives in `theorem-harness-runtime::memory` (`list_memory_documents_since`, `upsert_note`, `memory_content_hash`); 39 runtime + 41 MCP + 157 server tests green. Device-side plugin is `apps/obsidian-sync/` (TS, typecheck + esbuild bundle green). Confirmed build decisions: folder-or-flag capture scope, record-unresolved dangling links, conflict-copy default, add `upsert_note`. Spec divergences (code-grounded, documented in the plugin README): tenant is path-scoped not token-scoped (auth maps token->scopes, not tenant); edits use in-place `upsert_note` not `self_revise` (which mints a new doc_id); link edge is `MEMORY_RELATES` not `LINKS_TO`. Plan: `docs/plans/obsidian-sync/`.
- **Reflexive RustyRed (learned organs)**: implemented through the completion pass on `Travis-Gilbert/Reflexive-red`. The shared invariant everywhere: learned code ranks/steers within bounded enumerated spaces, never authors graph mutations. In `rustyred-thg-adapters`: `pairformer` (bounded dense triangle-update core), `edge_mpnn` (sparse NBFNet-style global completion scorer with pluggable `MessageAggregator`; the deterministic fixed-point aggregation is the consumed default), `burn_mpnn` behind the `pairformer-burn-cubecl` feature (Burn select/scatter parity-tested vs the fixed-point oracle; CubeCL scatter kernels actually LAUNCHED over wgpu with a float-atomic capability probe), `reflexive` (densification + quarantine; inferred edges are advisory candidate NODES, never topology), `reflexive_executor` (sidecar join via `REPRESENTS_NODE`, GraphLoRA-style low-rank factor sidecar + apply, pure `score_match_neighborhood`). In `rustyred-thg-server`: `PublicCypherBody.reflexive_inference` opts var-length/chain MATCH into an advisory `reflexive` response block (sidecar-joined Pairformer, rows untouched), and the Bao-style steered optimizer runs live for single-hop edge MATCH (candidate enumeration anchor-left/anchor-right, `PlanSteeringState` observations on `AppState`, cold-start abstention, decision in response stats). Context scorer receipts round-trip (`record_context_use_outcome` + `enrich_context_candidates_from_store`). Default-path weights are deterministic hash-seeded floors; the TRAINABLE Pairformer now exists as `burn_pairformer` behind the same feature (+`autodiff`): real Param weights in the AF3 block structure, self-supervised masked-edge training (the graph is its own label source), seeded/reproducible, BinFileRecorder persistence with ModelArtifact/EvaluationReceipt registry, and a learning-proof test (planted composition rule, held-out closures, trained beats untrained). Notes: `docs/plans/reflexive-rustyred/implementation-notes.md`.
- **jobintel (job-intelligence + outreach CLI)**: 0.1 implemented per `~/Downloads/jobintel-build-spec.md` as `apps/jobintel` (standalone Cargo root, light HTTP consumer of RustyRed, no substrate path-deps). All 5 0.1 modules built: ingest (HN Algolia `search_by_date` thread discovery + Greenhouse/Lever/Ashby ATS auto-detect), graph write (Company/Role/Skill/Source/Person + the 4 spec edge types plus reverse `posted_by`/`required_by` so directed PPR mass flows Profile->Skill->Role->Company), rank (vector + PPR/PageRank + flag blend), contacts (`ContactFinder`/`HunterFinder`, gated on `HUNTER_API_KEY`), draft (`context/pack` + `out/queue.md`). `Embedder` seam: `hash` (offline default), `http` (Theseus SBERT swap), `bge` (candle bge-small-en-v1.5, feature `bge`, compiles against candle 0.8.4). 0.1 live RustyRed proof on 2026-06-16: real ingest against `https://rustyredcore-theorem-production.up.railway.app` (tenant `jobintel`) wrote 1496 nodes / 6665 edges from 994 records; `rank --profile travis` produced ranked roles.
- **jobintel 0.2 (Outreach Engine)**: implemented per `~/Downloads/jobintel-0.2-spec.md` as `apps/jobintel/src/outreach/` (uncommitted as of 2026-06-16). All 5 0.2 modules built: state machine on the Role graph (`state.rs`: read-modify-write status since RustyRed upsert REPLACES nodes, append-only `OutreachEvent` trail, `queue`/`due_for_followup`), Gmail create-draft bridge (`gmail.rs` + `draft.rs`: `DraftSink`/`GmailDraftSink`, never sends, second HTTP seam base-URL-swappable; template set keyed by lead type from `templates/outreach/*.txt`, rendered deterministically with the role's own language + fixed proof block), cadence (`cadence.rs`: `select_for_draft` capped at `DAILY_DRAFT_CAP=8`, day-4/day-9 two-nudge schedule then reap to `dead`), reply detection (`sync.rs`: `ReplySource`, matches lead address in thread -> `replied`; advances `drafted->sent` by detecting the draft left the drafts list), learning signal (`outcomes.rs`: `OutcomeRecord` + `outreach stats` reply rate per template/lead-type). CLI: `outreach queue|draft|sync|followups|stats` plus operator helpers `trail` (reconstruct event trail from graph) + `mark`. Config adds `GMAIL_TOKEN` (token or path), `DAILY_DRAFT_CAP`, `FOLLOWUP_DAYS`, `JOBINTEL_GMAIL_API`. Validation: 139 tests green (75 unit + 6 + 58 integration), clippy clean, `cargo check --features bge` green; the two riskiest seams (Gmail create-draft + the read-modify-write that preserves title/body/embedding) proven against mock servers in `tests/outreach_seams.rs`. Code-grounded divergences (upsert is replace-not-merge so status is read-modify-write; `sent` is detected not commanded; deterministic render not model-filled; `contract_explicit` precedence) documented in the crate README. No live Gmail proof (needs the operator's real token). Deferred to 0.3 (named in spec): reply-rate -> rank-weight feedback, template A/B.
- **Reconciliation** between the Rust-theorem, RustyWeb, and kernel-object lanes: `docs/plans/commonplace-substrate-reconciliation/`.
