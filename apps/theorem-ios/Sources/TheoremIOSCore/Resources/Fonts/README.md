# Font Assets

This directory is a local drop zone for font testing.

Do not commit commercial Akzidenz-Grotesk font files unless the project has an app-embedding license. The package `.gitignore` keeps `.ttf`, `.otf`, and `.ttc` files local by default.
