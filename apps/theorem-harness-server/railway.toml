[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/theorem-harness-server/Dockerfile"

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
