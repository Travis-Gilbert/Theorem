# Rust build output (standalone workspace target dir).
/target

# Generated UniFFI bindings: regenerate with the uniffi-bindgen command in the
# README. The source of truth is src/lib.rs, not the generated Swift.
/generated

# Compiled Swift smoke and any native library copy: build artifacts.
smoke_swift
*.dylib
