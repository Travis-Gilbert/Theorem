const config = {
  plugins: {
    // Tailwind v4 ships its PostCSS plugin as a separate package and bundles
    // autoprefixer internally.
    "@tailwindcss/postcss": {},
  },
};

export default config;
