# File icons — attribution

These file-type icons are from the **a-file-icon** set by Elior "Mallowigi" Boukhobza.
Source: https://github.com/AtomMaterialUI/a-file-icon-idea
License: MIT (see LICENSE in this folder; the project's package.json also declares ISC — both permissive).

Vendored `*.svg`: only the file types used by the app (markdown, image, typescript, js, react, python, rust, go, json, toml, yaml, pdf, video, css, html, file).

Brand/provider logos: `redis.svg`, `postgresql.svg`, `snowflake.svg` (from `icons/providers/`), `webassembly.svg` (the pack's `files/plugins/rust/wasmPack.svg`). Used on the "Programmable" cards and in the DocTree (snowflake = cold storage).
