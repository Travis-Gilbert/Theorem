# SwiftPM build artifacts + local state — never commit these.
.build/
.swiftpm/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
.DS_Store
# Package.resolved IS committed (this is an app — pin dependency versions).
