# Theorem Reconstruction Engine

## Goal

Compile a binary artifact into versioned GraphStore facts and bounded reconstruction obligations. The engine is not a human decompiler surface. Agents receive `ReconstructionInstruction` nodes with evidence and validators, then write validation receipts after rebuilding equivalent behavior.

## Review Artifacts

- [`ghidra-reference-scout.md`](ghidra-reference-scout.md) records the Ghidra reference scout and next oracle/parity cuts.
- [`build-ledger.md`](build-ledger.md) lists the built reconstruction/code-oracle features in review-table form.

## Current Native Slice

- `rustyred-thg-binformat`: parses object files with `object`, extracts artifact, section, symbol, string, relocation, entrypoint, and Ghidra-inspired language-pack fact models including compiler-spec callfixup and callotherfixup p-code payloads, and writes observed facts into a ProgramGraph ontology in GraphStore.
- `rustyred-thg-disasm`: decodes x86-64 executable sections with `iced-x86`, emits `InstructionFact` nodes with flow-control, branch targets, bytes, and coarse effects, and records Ghidra BytePatterns-inspired function-start candidate evidence.
- `rustyred-thg-lift`: lowers instruction facts into minimal THIR functions, basic blocks, SSA-like statements, and Ghidra p-code-aligned semantic facts, including raw THIR statements whose first token is a known Ghidra p-code mnemonic such as `CALLOTHER`.
- `rustyred-thg-code`: records Ghidra-inspired program-analysis run contracts, analysis pass specs, changed-range scheduler work items, oracle fixtures, headless oracle export JSON, SymbolicSummaryZ3, decompiler-diagnostic, BSim/decompiler-signature, and FunctionID headless exporters, summary and address-level oracle drift receipts, p-code/reference/call-edge/jump-table/equate/external-linkage/data-type/structure-field-access/high-variable/stack-frame/call-stack-effect/function-prototype/annotation oracle facts with Ghidra reference semantic roles, SymbolicSummaryZ3-compatible oracle symbolic summaries, Ghidra decompiler diagnostic facts, Ghidra BSim semantic-signature facts, Ghidra FunctionID hash facts, Ghidra jump-table case/default/load-table facts, Ghidra equate/named-constant facts, Ghidra external import/thunk-chain facts, Ghidra data-type/field/enum/dependency facts, Ghidra decompiler field/bitfield token facts, Ghidra high-symbol/high-variable storage facts, Ghidra stack-frame/stack-variable storage facts, Ghidra `CallDepthChangeInfo` callsite stack-delta facts, Ghidra `Function` prototype/return/parameter/storage/signature-source facts, Ghidra `Listing` comment and `BookmarkManager` annotation facts, reference recovery evidence, runtime trace and taint contracts, behavior/spec obligations, and engineering validation receipts for repo/site/API/binary reconstruction.
- `rustyred-thg-reconstruct`: derives basic semantic-role hypotheses, emits BSim-inspired THIR semantic function signatures, lowers Ghidra BSim oracle signatures into the same `FunctionSemanticSignature` lane, emits FunctionID-inspired identifier/hash-family facts, lowers Ghidra FunctionID oracle hash quads into the same `FunctionIdentifierSignature` lane, accepts Ghidra-style typed reference semantic features, function prototype contracts, data-type layout/dependency contracts, structure field-access contracts, stack-frame layout contracts, call-stack-effect/extra-pop contracts with derived `ReferenceOverrideKind::ExtraPop` flow overrides, decompiler diagnostic/uncertainty contracts, lowers Ghidra oracle diagnostics and Ghidra comment/bookmark annotations into those diagnostic contracts, high-variable storage contracts, ParamID/parameter-measure contracts, p-code injection/callfixup/callotherfixup contracts with Ghidra-style callfixup target binding against recovered functions and symbols, callotherfixup binding to concrete `CALLOTHER` userop p-code sites, non-fallthrough callfixup flow repair into `FlowOverrideContract`s, primary Ghidra reference-override lowering for call, jump, CALLOTHER call/jump, and fallthrough overrides, localized prototype override lowering from Ghidra-style callsite `prt`/`proto_override` symbols, Ghidra jump-table override namespace lowering from `jmp_<switch>`/`switch`/`case_*`/`format_*` symbols into switch-dispatch contracts, Ghidra jump-table oracle lowering with default-case marker and load-table preservation, Ghidra equate oracle lowering with operand-index and dynamic-hash preservation, Ghidra external-linkage oracle lowering with external address and thunk-chain preservation, Ghidra data-type oracle lowering into `DataTypeContract`s, Ghidra structure-field-access oracle lowering into `StructureFieldAccessContract`s, Ghidra high-variable oracle lowering into `HighVariableContract`s, Ghidra stack-frame oracle lowering into `StackFrameContract`s, Ghidra call-stack-effect oracle lowering into `CallStackEffectContract`s, Ghidra function-prototype oracle lowering into `FunctionPrototypeContract`s, p-code execution/replay/taint/symbolic-precondition/symbolic-state contracts, flow/prototype override contracts, variable-merge/cover contracts, decompiler action/rule application contracts, jump-table/switch-dispatch contracts, equate/named-constant contracts, and external-linkage/thunk contracts as recovery evidence, records VersionTracking-inspired function match receipts, recovers component hypotheses, compiles evidence-backed reconstruction instructions, and records validation receipts.
- `rustyred-thg-reconstruct-harness`: exposes the `theorem.reconstruct.binary` capability pack and plugin operations for load, analyze, lift, graph write, component recovery, plan compile, instruction get, validation, and receipt write, with pipeline reports including function-signature counts. Operations accept a binary either as `bytes_hex` or as an http(s) `url` (fetched with a scheme guard, a global timeout, and a hard size cap); `fetch_binary_bytes` and `run_reconstruction_pipeline_from_url` are the URL front door, so a reverse-engineer caller with only a URL reaches reconstruction without pre-downloading and hex-encoding. SSRF hardening (private/link-local IP blocking) and server-side wiring through `theorem-grpc` are named follow-ups.
- `rustyred-thg-mcp`: exposes the agent-facing `reconstruct_binary` umbrella tool with operation selection and read-only gating.

## Authority Layers

- `observed_fact`: loader, decoder, and lifter output.
- `derived_fact`: symbolic/e-graph facts once rules are attached.
- `hypothesis`: semantic roles and component groups.
- `instruction`: pending reconstruction obligations.
- `validated_instruction`: obligations whose validators pass.
- `accepted_reconstruction`: future canonical merge after review.

## Follow-On Work

- Extract real relocation records from object sections.
- Add Datalog/egglog rules as first-class derived-fact passes instead of the current basic semantic recognizers.
- Replace component recovery heuristics with learned GNN boundary detection when the training stream is available.
- Feed program-analysis trace and taint facts directly into the p-code execution, symbolic-precondition, and symbolic-state contracts so native traces, Ghidra oracle replay, symbolic branch conditions, and register/memory valuation summaries share one validator surface. SymbolicSummaryZ3 oracle summaries now lower directly into reconstruction p-code execution contracts, and `ExportTheoremSymZ3Facts.java` can produce those summaries from Ghidra's symbolic emulator.
- Run the checked-in generic, SymZ3, decompiler-diagnostics, BSim-signature, and FunctionID Ghidra headless exporters against real fixture binaries and expand parity tests for more instruction families, branch paths, memory witnesses, diagnostic categories, semantic-signature settings, FunctionID hash quads, library matches, jump-table case/default/load-table facts, equate enum/dynamic-hash references, data-type/field/enum facts, structure field-access token facts, high-variable symbol/storage/instance facts, stack-frame/call-stack-effect/function-prototype/comment/bookmark annotation facts, similarity matches, and architectures.
- Feed real Ghidra `.ldefs`, `.pspec`, and `.cspec` files through `ghidra_language_specs_from_pack` or `ghidra_language_specs_from_directory` so language/compiler/processor specs and callfixup/callotherfixup p-code bodies come from source-grounded processor packs rather than hand-maintained snapshots.
- Use the Ghidra-exact `PcodeOpcode` metadata in `rustyred-thg-lift` when deriving reconstruction evidence: prefer opcode IDs, `ghidra_mnemonic`, category, and control-flow/memory/internal/commutative flags over string matching in downstream passes.
- Extend `CallOtherFixup` site binding with real Ghidra oracle userop-index parity so numeric CALLOTHER IDs, language-pack userop names, and native THIR p-code facts round-trip across x86 and non-x86 fixtures.
- Expand call-depth stack-delta parity against real Ghidra effective-extrapop oracle exports across calling conventions, varargs, no-return callfixups, and non-x86 stack models.
- Extend `flow_override_contracts_from_non_fallthrough_pcode_injects` from typed call-reference repair into thunk-chain repair and richer import/callsite recovery, matching Ghidra's recursive thunk handling around non-fallthrough callfixups and no-return targets.
- Use the Ghidra reference scout in [`ghidra-reference-scout.md`](ghidra-reference-scout.md) to guide the next oracle/parity lane, especially richer ProgramGraph ontology, analyzer scheduling, richer language-pack/SLEIGH ingestion, richer p-code/THIR parity, richer p-code execution/taint replay parity, richer BSim parity, richer VersionTracking parity, data-type/structure-field-access/stack-frame/call-stack-effect/function-prototype/decompiler-diagnostic/comment-bookmark-annotation/high-variable/ParamID/p-code-injection/flow-override/variable-merge/decompiler-rule/jump-table/external-linkage oracle exports, and trace/taint contracts.
